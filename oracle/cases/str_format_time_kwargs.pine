//@version=5
indicator("str_format_time_kwargs")
// C478: str.format_time(timezone=) kwarg 신규 지원(next_hint(C477) 1순위 -- wild 잔여 '키워드
// 인자' 블랑켓 최다빈도, 2942202a2b71/82212d1b1679/c6df7b067b6f 3파일). 'timezone'만 pine2py
// wavealgo/builtins/str_funcs.py format_time의 실제 파라미터명과 일치해 오라클 가능(timezone은
// 함수 본문에서 전혀 참조되지 않는 no-op이라 값은 항상 위치 인자 폼과 동일한 문자열, LIMITATIONS.md
// C112 참조). 'time'/'format' 키워드 폼(fab0b2ffba07.pine)은 pine2py 내부 파라미터명
// (time_ms/format_str) 불일치로 오라클 구조적 불가 -- codegen 동치성 hand-verified로 별도 대체
// (tests/unit/codegen.test.ts 참조).
var string __obs_kw_tz_positional_format = na
__obs_kw_tz_positional_format := str.format_time(1705276800000.0, "yyyy-MM-dd", timezone="America/New_York")
var string __obs_kw_tz_default_format = na
__obs_kw_tz_default_format := str.format_time(1705329045000.0, timezone="UTC")
