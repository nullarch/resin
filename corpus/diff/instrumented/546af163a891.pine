
//@version=5
indicator("Risk Target")
atr = ta.atr(3)
targetRisk = 2.0
posSize = 1.0
iters = 0
while close > 0 and atr * posSize / close * 100 > targetRisk and iters < 20
    posSize := posSize * 0.8
    iters := iters + 1
var float __obs_plot0 = na
__obs_plot0 := posSize
plot(posSize, "Size")
