
//@version=5
indicator("Fix Frac")
capital = 10000.0
riskPct = 1.5
atr = ta.atr(3)
stopDist = atr * 2
riskAmt = capital * riskPct / 100
units = stopDist > 0 ? math.floor(riskAmt / stopDist) : 0
var float __obs_plot0 = na
__obs_plot0 := units
plot(units, "Units")
