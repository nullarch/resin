//@version=5
indicator("MACD Color")
[macdLine, signalLine, hist] = ta.macd(close, 3, 5, 3)
histColor = hist >= 0 ?
    (hist[1] < hist ? color.green : color.lime) :
    (hist[1] < hist ? color.red : color.maroon)
var float __obs_plot0 = na
__obs_plot0 := hist
plot(hist, color=histColor)
