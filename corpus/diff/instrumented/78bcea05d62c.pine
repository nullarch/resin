//@version=5
indicator("Entropy Pool")
var float pool = 0.0
newEntropy = math.abs(close - open) * 7.3 + math.abs(high - low) * 3.1
pool := pool + newEntropy
pool := pool - math.floor(pool / 1000.0) * 1000.0
var float __obs_plot0 = na
__obs_plot0 := pool
plot(pool)
