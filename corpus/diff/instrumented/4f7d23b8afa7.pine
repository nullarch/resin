//@version=5
indicator("FitMaxDD")
var float peak = 0.0
var float maxDD = 0.0
peak := math.max(peak, close)
dd = peak > 0 ? (peak - close) / peak : 0.0
maxDD := math.max(maxDD, dd)
fitness = 1.0 - maxDD
var float __obs_plot0 = na
__obs_plot0 := fitness
plot(fitness, "Fitness")
