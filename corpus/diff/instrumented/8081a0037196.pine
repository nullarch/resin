//@version=5
indicator("NestedIfForTernArr")
arr = array.new_float(0)
if close > open
    for i = 0 to 3
        val = i > 1 ? 2.0 : 1.0
        array.push(arr, val)
else
    array.push(arr, 0.0)
var float __obs_plot0 = na
__obs_plot0 := float(array.size(arr))
plot(float(array.size(arr)), "Sz")
