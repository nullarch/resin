//@version=5
indicator("Realized Var")
float prevC = not na(close[1]) ? close[1] : close
float logRet = prevC > 0 ? math.log(close / prevC) : 0.0
float sqLogRet = logRet * logRet
float realVar = ta.sma(sqLogRet, 5)
var float __obs_plot0 = na
__obs_plot0 := realVar
plot(realVar, "RealVar")
