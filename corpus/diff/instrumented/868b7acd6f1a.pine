
//@version=5
indicator("MeanRev")
smaVal = nz(ta.sma(close, 5), close)
atrVal = nz(ta.atr(3), 1.0)
zProxy = atrVal > 0.001 ? (close - smaVal) / atrVal : 0.0
signal = zProxy > 2.0 ? -1 : zProxy < -2.0 ? 1 : 0
var float __obs_plot0 = na
__obs_plot0 := signal
plot(signal, "S")
