
//@version=5
indicator("Atan2")
dx = close - close[1]
dy = high - low
angle = math.atan2(nz(dx, 0.0), dy > 0 ? dy : 1.0)
degrees = angle * 180 / math.pi
var float __obs_plot0 = na
__obs_plot0 := degrees
plot(degrees, "Deg")
