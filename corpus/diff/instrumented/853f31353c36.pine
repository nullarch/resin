//@version=5
indicator("RSI + MA")
rsi = ta.rsi(close, 5)
rsiMA = ta.sma(rsi, 3)
overbought = rsi > 70
oversold = rsi < 30
signal = overbought ? -1 : oversold ? 1 : 0
var float __obs_plot0 = na
__obs_plot0 := signal
plot(signal)
