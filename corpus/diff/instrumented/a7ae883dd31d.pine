//@version=5
indicator("test")
var float[] arr = array.new_float(0)
if barstate.isfirst
    array.push(arr, 10.0)
    array.push(arr, 20.0)
    array.push(arr, 30.0)
var float total = 0.0
total := 0.0
for [idx, val] in arr
    total := total + val
var float __obs_plot0 = na
__obs_plot0 := total
plot(total)
