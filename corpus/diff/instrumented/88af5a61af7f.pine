//@version=5
indicator("Anchoring")
var float anchor = na
if na(anchor)
    anchor := close
deviation = close - nz(anchor)
pctDev = nz(anchor) > 0 ? deviation / nz(anchor) * 100.0 : 0.0
var float __obs_plot0 = na
__obs_plot0 := pctDev
plot(pctDev)
