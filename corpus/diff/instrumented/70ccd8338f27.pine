
//@version=5
indicator("Dev")
sma5 = ta.sma(close, 5)
stdev = nz(ta.stdev(close, 5), 1.0)
dev = stdev > 0 ? (close - sma5) / stdev : 0.0
extreme = math.abs(dev) > 2.0
var float __obs_plot0 = na
__obs_plot0 := dev
plot(dev, "Dev")
