
//@version=5
indicator("BidAskSpread")
spread = high - low
relSpread = close > 0 ? spread / close * 100.0 : 0.0
avgSpread = ta.sma(relSpread, 5)
var float __obs_plot0 = na
__obs_plot0 := nz(avgSpread, 0.0)
plot(nz(avgSpread, 0.0), "Spread")
