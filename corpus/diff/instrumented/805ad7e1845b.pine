
//@version=5
indicator("ATR Bands")
sma5 = ta.sma(close, 5)
atr = nz(ta.atr(3), 1.0)
upper = sma5 + atr * 1.5
lower = sma5 - atr * 1.5
var float __obs_plot0 = na
__obs_plot0 := close > upper ? 1 : close < lower ? -1 : 0
plot(close > upper ? 1 : close < lower ? -1 : 0, "Band")
