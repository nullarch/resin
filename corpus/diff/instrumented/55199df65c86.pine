
//@version=5
indicator("MTF Sim")
bullCount = 0
for period = 3 to 5
    ma = ta.sma(close, period)
    if close > ma
        bullCount := bullCount + 1
consensus = bullCount >= 2
var float __obs_plot0 = na
__obs_plot0 := consensus ? 1 : 0
plot(consensus ? 1 : 0, "C")
