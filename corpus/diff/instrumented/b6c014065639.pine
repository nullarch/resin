
//@version=5
indicator("Vol Ratio")
atr = nz(ta.atr(3), 1.0)
avgATR = ta.sma(nz(ta.atr(3), 1.0), 5)
ratio = avgATR > 0 ? atr / avgATR : 1.0
highVol = ratio > 1.5
lowVol = ratio < 0.5
var float __obs_plot0 = na
__obs_plot0 := highVol ? 2 : lowVol ? 0 : 1
plot(highVol ? 2 : lowVol ? 0 : 1, "VR")
