
//@version=5
indicator("Contains")
rsi = nz(ta.rsi(close, 3), 50.0)
label = rsi > 60 ? "BULL_STRONG" : rsi > 50 ? "BULL_WEAK" : "BEAR"
hasBull = str.contains(label, "BULL")
var float __obs_plot0 = na
__obs_plot0 := hasBull ? 1 : 0
plot(hasBull ? 1 : 0, "Bull")
