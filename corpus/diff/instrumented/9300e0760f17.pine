//@version=5
indicator("NS-05 Pressure Gradient")
var float pressure = 0.0
if not na(close[1])
    priceChange = close - close[1]
    pressure := priceChange * volume / 1000.0
else
    pressure := 0.0
var float __obs_plot0 = na
__obs_plot0 := pressure
plot(pressure)
