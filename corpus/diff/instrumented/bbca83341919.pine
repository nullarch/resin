
//@version=5
indicator("Drawdown")
var float peak = 0.0
if close > peak
    peak := close
drawdown = peak > 0 ? (peak - close) / peak * 100 : 0.0
var float __obs_plot0 = na
__obs_plot0 := drawdown
plot(drawdown, "DD")
var float __obs_plot1 = na
__obs_plot1 := peak
plot(peak, "Peak")
