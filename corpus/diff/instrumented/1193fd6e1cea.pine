//@version=5
indicator("Array Stats")
var prices = array.new_float(0)
array.push(prices, close)
if array.size(prices) > 5
    array.shift(prices)
avg_ = array.avg(prices)
var float __obs_plot0 = na
__obs_plot0 := avg_
plot(avg_)
