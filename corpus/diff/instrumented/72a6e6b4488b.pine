
//@version=5
indicator("Consec")
bullCount = 0
for i = 0 to 4
    if not na(close[i]) and not na(open_[i])
        if close[i] > open_[i]
            bullCount := bullCount + 1
        else
            bullCount := 0
var float __obs_plot0 = na
__obs_plot0 := bullCount
plot(bullCount, "BC")
