//@version=5
indicator("Math Comp")
sq = math.sqrt(close)
lg = math.log(close)
pw = math.pow(close / 100, 2)
sn = math.sign(close - 107)
fl = math.floor(sq)
cl = math.ceil(sq)
var float result = 0.0
result := sq + lg + pw + sn + fl + cl
var float __obs_plot0 = na
__obs_plot0 := close
plot(close)
