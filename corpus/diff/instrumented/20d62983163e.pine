//@version=5
indicator("Congestion Window")
float cwnd = nz(ta.ema(volume, 3), volume)
float ratio = volume > 0 ? cwnd / volume : 1.0
var float __obs_plot0 = na
__obs_plot0 := ratio
plot(ratio, "CwndRatio")
