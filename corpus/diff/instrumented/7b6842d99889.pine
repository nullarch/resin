
//@version=5
indicator("Vol Conv")
atr = nz(ta.atr(3), 1.0)
estimate = atr * 2
target = atr
iters = 0
while math.abs(estimate - target) > 0.01 and iters < 30
    estimate := estimate * 0.9 + target * 0.1
    iters := iters + 1
var float __obs_plot0 = na
__obs_plot0 := iters
plot(iters, "It")
