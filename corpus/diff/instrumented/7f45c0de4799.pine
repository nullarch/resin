
//@version=5
indicator("Softmax")
rsi = nz(ta.rsi(close, 3), 50.0)
smaVal = ta.sma(close, 3)
diff = close - smaVal
e1 = math.exp(rsi / 50.0)
e2 = math.exp(diff)
total = e1 + e2
p1 = total > 0 ? e1 / total : 0.5
var float __obs_plot0 = na
__obs_plot0 := p1
plot(p1, "P1")
