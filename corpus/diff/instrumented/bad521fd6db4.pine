
//@version=5
indicator("Rebal")
atr = nz(ta.atr(3), 1.0)
threshold = 5.0
steps = 0
drift = atr / close * 100
while threshold > drift * 2 and steps < 10
    threshold := threshold * 0.8
    steps := steps + 1
var float __obs_plot0 = na
__obs_plot0 := threshold
plot(threshold, "Th")
