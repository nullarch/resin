
//@version=5
indicator("Streak")
var int upStreak = 0
var int downStreak = 0
var int maxUp = 0
var int maxDown = 0
if close > close[1]
    upStreak := upStreak + 1
    downStreak := 0
else if close < close[1]
    downStreak := downStreak + 1
    upStreak := 0
if upStreak > maxUp
    maxUp := upStreak
if downStreak > maxDown
    maxDown := downStreak
var float __obs_plot0 = na
__obs_plot0 := upStreak
plot(upStreak, "UpStreak")
var float __obs_plot1 = na
__obs_plot1 := maxUp
plot(maxUp, "MaxUp")
