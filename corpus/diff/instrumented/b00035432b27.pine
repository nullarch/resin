
//@version=5
indicator("Empty Arr")
var float[] arr = array.new_float(0)
sz = array.size(arr)
avg = sz > 0 ? array.avg(arr) : 0.0
array.push(arr, close)
if array.size(arr) > 3
    array.shift(arr)
var float __obs_plot0 = na
__obs_plot0 := avg
plot(avg, "A")
