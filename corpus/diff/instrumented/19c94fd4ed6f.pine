//@version=5
indicator("UDT Fields")
type Position
    float entry = 0.0
    float stop = 0.0
    bool active = false

var pos = Position.new()
if close > 106
    pos := Position.new(close, close - 2, true)

var float last_entry = 0.0
last_entry := pos.entry
var float __obs_plot0 = na
__obs_plot0 := close
plot(close)
