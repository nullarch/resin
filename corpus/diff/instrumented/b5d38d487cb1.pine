//@version=5
indicator("Even Odd")
idx = math.floor(bar_index)
remainder = idx - math.floor(idx / 2.0) * 2.0
isEven = remainder == 0.0 ? 1.0 : 0.0
var float __obs_plot0 = na
__obs_plot0 := isEven
plot(isEven, "IsEven")
