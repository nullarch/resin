
//@version=5
indicator("Map KV")
var m = map.new<string, float>()
map.put(m, "sma", ta.sma(close, 3))
map.put(m, "ema", ta.ema(close, 3))
map.put(m, "rsi", ta.rsi(close, 3))
ks = map.keys(m)
vs = map.values(m)
numIndicators = array.size(ks)
var float __obs_plot0 = na
__obs_plot0 := numIndicators
plot(numIndicators, "Count")
