//@version=5
indicator("TA Chain Complex")
length = 10
val = ta.linreg(close - math.avg(
    math.avg(ta.highest(high, length), ta.lowest(low, length)),
    ta.sma(close, length)), length, 0)
var float __obs_plot0 = na
__obs_plot0 := val
plot(val)
