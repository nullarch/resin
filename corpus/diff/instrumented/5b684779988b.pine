
//@version=5
indicator("Amihud")
ret = close > 0 and close[1] > 0 ? math.abs(close - close[1]) / close[1] : 0.0
illiq = volume > 0 ? ret / volume * 1000000 : 0.0
avgIlliq = ta.sma(illiq, 5)
var float __obs_plot0 = na
__obs_plot0 := avgIlliq
plot(avgIlliq, "Illiq")
