
//@version=5
indicator("WeightUpdate")
var float weight = 0.5
learningRate = 0.01
prediction = weight * close
target = close[1]
if not na(target)
    err = target - prediction
    weight := weight + learningRate * err * 0.001
var float __obs_plot0 = na
__obs_plot0 := weight
plot(weight, "W")
