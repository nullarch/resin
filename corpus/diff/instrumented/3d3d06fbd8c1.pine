//@version=5
indicator("Homology Dimension RSI")
float rsiVal = nz(ta.rsi(close, 3), 50.0)
float dim = rsiVal > 70 ? 2.0 :
    rsiVal > 50 ? 1.0 : 0.0
var float __obs_plot0 = na
__obs_plot0 := dim
plot(dim, "Dim")
