
//@version=5
indicator("Pivots")
ph = ta.pivothigh(high, 2, 2)
pl = ta.pivotlow(low, 2, 2)
hasPivot = not na(ph) or not na(pl)
var float __obs_plot0 = na
__obs_plot0 := hasPivot ? 1 : 0
plot(hasPivot ? 1 : 0, "HasPivot")
