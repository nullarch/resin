
//@version=5
indicator("MA Dist")
sma5 = ta.sma(close, 5)
totalDist = 0.0
for i = 0 to 4
    if not na(close[i])
        totalDist := totalDist + math.abs(close[i] - sma5)
avgDist = totalDist / 5
var float __obs_plot0 = na
__obs_plot0 := avgDist
plot(avgDist, "AD")
