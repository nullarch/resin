//@version=5
indicator("LCM")
a = math.floor(math.abs(close))
b = math.floor(math.abs(high))
minVal = math.min(a, b)
maxVal = math.max(a, b)
gcd = minVal > 0.0 ? minVal : maxVal
lcmVal = gcd > 0.0 ? a * b / gcd : 0.0
var float __obs_plot0 = na
__obs_plot0 := lcmVal
plot(lcmVal, "LCM")
