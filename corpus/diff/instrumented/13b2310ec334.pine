//@version=5
indicator("Halting Heuristic")
var int steps = 0
var bool halted = false
steps := steps + 1
if steps > 5
    halted := true
running = halted ? 0.0 : 1.0
var float __obs_plot0 = na
__obs_plot0 := running
plot(running, "Running")
