
//@version=5
indicator("SSI")
total = 0.0
for scale = 2 to 5
    v = ta.stdev(close, scale)
    total := total + nz(v, 0.0)
ssi = total > 0 ? total / 4.0 : 0.0
var float __obs_plot0 = na
__obs_plot0 := ssi
plot(ssi, "SSI")
