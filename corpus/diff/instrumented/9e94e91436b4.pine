//@version=5
indicator("Firefly Sync")
var float phase = 0.0
emaVal = ta.ema(close, 5)
coupling = 0.1
diff = close - emaVal
phase := phase + 0.2 + coupling * diff
flash = phase > 6.28 ? 1 : 0
if phase > 6.28
    phase := phase - 6.28
var float __obs_plot0 = na
__obs_plot0 := flash
plot(flash)
