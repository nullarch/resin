
//@version=5
indicator("Sess Map")
var sm = map.new<string, float>()
var int barNum = 0
barNum := barNum + 1
map.put(sm, "bar", float(barNum))
map.put(sm, "position", float(barNum % 5))
map.put(sm, "close", close)
total = 0.0
for [k, v] in sm
    total := total + v
var float __obs_plot0 = na
__obs_plot0 := total
plot(total, "T")
