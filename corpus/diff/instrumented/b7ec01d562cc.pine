
//@version=5
indicator("Vol Div")
priceUp = close > close[1]
volDown = volume < volume[1]
negDiv = priceUp and volDown
posDiv = not priceUp and not volDown
var float __obs_plot0 = na
__obs_plot0 := negDiv ? -1 : posDiv ? 1 : 0
plot(negDiv ? -1 : posDiv ? 1 : 0, "VD")
