
//@version=5
indicator("Lower Shd")
botBody = math.min(close, open)
lowerShadow = botBody - low
totalRange = high - low
shadowRatio = totalRange > 0 ? lowerShadow / totalRange * 100 : 0.0
var float __obs_plot0 = na
__obs_plot0 := shadowRatio
plot(shadowRatio, "LS")
