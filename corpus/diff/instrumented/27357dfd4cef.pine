
//@version=5
indicator("Ternary Bool")
sma3 = ta.sma(close, 3)
rsiVal = ta.rsi(close, 3)
strongSignal = close > sma3 ? (rsiVal > 60 ? true : false) : (rsiVal < 40 ? true : false)
var float __obs_plot0 = na
__obs_plot0 := strongSignal ? 1 : 0
plot(strongSignal ? 1 : 0, "Strong")
