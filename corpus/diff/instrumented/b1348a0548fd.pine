
//@version=5
indicator("RSProxy")
length = 5
rangeHL = ta.highest(high, length) - ta.lowest(low, length)
dev = ta.stdev(close, length)
rs = dev > 0 ? rangeHL / dev : 0.0
var float __obs_plot0 = na
__obs_plot0 := rs
plot(rs, "RS")
