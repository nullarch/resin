//@version=5
indicator("Map Contains")
var m = map.new<string, float>()
map.put(m, "x", 42.0)
var bool has_x = map.contains(m, "x")
map.remove(m, "x")
var bool after = map.contains(m, "x")
var float val = has_x ? 1.0 : 0.0
var float __obs_plot0 = na
__obs_plot0 := val
plot(val)
