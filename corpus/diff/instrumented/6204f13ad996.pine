//@version=5
indicator("Comprehensive 56")

// Array mutation
var arr = array.from(50.0, 10.0, 30.0, 20.0, 40.0)
array.sort(arr)
var float arr_min = array.get(arr, 0)
var float arr_max = array.get(arr, 4)

// Math
var float sq = math.sqrt(144.0)
var float pw = math.pow(2.0, 3.0)

// TA with isolation
sma3 = ta.sma(close, 3)
ema5 = ta.ema(close, 5)

// var persistence
var float cnt = 0.0
cnt := cnt + 1.0

// Drawing noop
var lbl = label.new(bar_index, close, "test")
label.set_text(lbl, "ok")

// Result
var float result = 0.0
result := arr_min + arr_max + sq + pw + cnt + nz(sma3, 0.0) + nz(ema5, 0.0)
var float __obs_plot0 = na
__obs_plot0 := close
plot(close)
