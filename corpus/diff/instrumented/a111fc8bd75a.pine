
//@version=5
indicator("MultiTF")
fastTrend = nz(ta.sma(close, 2), close)
slowTrend = nz(ta.sma(close, 5), close)
spread = fastTrend - slowTrend
alignment = spread > 0 ? 1.0 : -1.0
var float __obs_plot0 = na
__obs_plot0 := alignment
plot(alignment, "Align")
