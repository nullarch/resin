//@version=5
indicator("Stigmergy")
pivotHigh = ta.highest(high, 5)
pivotLow = ta.lowest(low, 5)
midPivot = (pivotHigh + pivotLow) / 2.0
attraction = midPivot - close
var float __obs_plot0 = na
__obs_plot0 := attraction
plot(attraction)
