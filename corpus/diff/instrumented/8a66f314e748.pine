
//@version=5
indicator("Mom Streak")
var int streak = 0
rsi = nz(ta.rsi(close, 3), 50.0)
if rsi > 50
    streak := streak > 0 ? streak + 1 : 1
else
    streak := streak < 0 ? streak - 1 : -1
var float __obs_plot0 = na
__obs_plot0 := streak
plot(streak, "Str")
