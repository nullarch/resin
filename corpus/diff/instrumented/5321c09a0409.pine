//@version=5
indicator("ScaleRatio")
fine = nz(ta.sma(close, 2), close)
coarse = nz(ta.sma(close, 5), close)
fineDev = math.abs(close - fine)
coarseDev = math.abs(close - coarse)
ratio = coarseDev > 0.01 ? fineDev / coarseDev : 1.0
var float __obs_plot0 = na
__obs_plot0 := ratio
plot(ratio, "SR")
