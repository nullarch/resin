
//@version=5
indicator("RSI Alert")
rsi = nz(ta.rsi(close, 3), 50.0)
overbought = rsi > 70
oversold = rsi < 30
alertcondition(overbought, "OB")
alertcondition(oversold, "OS")
var float __obs_plot0 = na
__obs_plot0 := overbought ? 1 : oversold ? -1 : 0
plot(overbought ? 1 : oversold ? -1 : 0, "A")
