//@version=5
indicator("Map Keys")
var m = map.new<string, float>()
map.put(m, "a", 10.0)
map.put(m, "b", 20.0)
var ks = map.keys(m)
var vs = map.values(m)
var int klen = array.size(ks)
var float vsum = array.sum(vs)
var float __obs_plot0 = na
__obs_plot0 := klen
plot(klen)
