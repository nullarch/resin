
//@version=5
indicator("Z-Score")
mean = ta.sma(close, 5)
sd = ta.stdev(close, 5)
zscore = sd > 0 ? (close - mean) / sd : 0.0
overbought = zscore > 2
oversold = zscore < -2
var float __obs_plot0 = na
__obs_plot0 := zscore
plot(zscore, "Z")
