
//@version=5
indicator("StartEnd")
tag = "SIGNAL_BUY_STRONG"
startsOk = str.startswith(tag, "SIGNAL")
endsOk = str.endswith(tag, "STRONG")
both = startsOk and endsOk
var float __obs_plot0 = na
__obs_plot0 := both ? 1 : 0
plot(both ? 1 : 0, "B")
