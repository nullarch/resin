//@version=5
indicator("Cross Detect")
fast = ta.sma(close, 3)
slow = ta.sma(close, 5)
crossUp = ta.crossover(fast, slow)
crossDn = ta.crossunder(fast, slow)
sig = crossUp ? 1 : crossDn ? -1 : 0
var float __obs_plot0 = na
__obs_plot0 := sig
plot(sig)
