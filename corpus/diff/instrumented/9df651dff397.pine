//@version=5
indicator("Cond Color")
rsi = ta.rsi(close, 5)
sma = ta.sma(close, 5)
bullish = close > sma and nz(rsi, 50) > 50
bearish = close < sma and nz(rsi, 50) < 50
c = bullish ? color.green : bearish ? color.red : color.gray
var float __obs_plot0 = na
__obs_plot0 := close
plot(close, color=c)
plotshape(bullish, style=shape.triangleup, location=location.belowbar, color=color.green)
plotshape(bearish, style=shape.triangledown, location=location.abovebar, color=color.red)
