//@version=5
indicator("Var Init")
var float x = close
// x is initialized to close on bar 0 (102), then never re-initialized
var float result = 0.0
result := x
var float __obs_plot0 = na
__obs_plot0 := result
plot(result)
