
//@version=5
indicator("Digit Sum")
n = int(close)
digitSum = 0
temp = n
while temp > 0
    digitSum := digitSum + temp % 10
    temp := int(temp / 10)
var float __obs_plot0 = na
__obs_plot0 := digitSum
plot(digitSum, "DigitSum")
