//@version=5
indicator("Wasserstein 1D")
float smaVal = nz(ta.sma(close, 3), close)
var float wDist = 0.0
float diff = math.abs(close - smaVal)
wDist := wDist + diff
float avgW = bar_index > 0 ? wDist / bar_index : 0.0
var float __obs_plot0 = na
__obs_plot0 := avgW
plot(avgW, "W1")
