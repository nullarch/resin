//@version=5
indicator("NS-17 Stream Function")
var float psi = 0.0
if not na(close[1])
    vx = close - close[1]
    vy = volume / 1000.0
    psi := psi + vx * vy
var float __obs_plot0 = na
__obs_plot0 := psi
plot(psi)
