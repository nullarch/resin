
//@version=5
indicator("Adapt Wt")
ema3 = ta.ema(close, 3)
sma5 = ta.sma(close, 5)
rsi = nz(ta.rsi(close, 3), 50.0)
emaWeight = rsi > 60 ? 0.7 : rsi < 40 ? 0.3 : 0.5
blended = ema3 * emaWeight + sma5 * (1.0 - emaWeight)
var float __obs_plot0 = na
__obs_plot0 := blended
plot(blended, "Blend")
