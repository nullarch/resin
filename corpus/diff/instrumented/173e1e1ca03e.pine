
//@version=5
indicator("KC Width")
basis = ta.ema(close, 5)
atr = ta.atr(3)
upper = basis + atr * 1.5
lower = basis - atr * 1.5
kcWidth = basis > 0 ? (upper - lower) / basis * 100 : 0.0
var float __obs_plot0 = na
__obs_plot0 := kcWidth
plot(kcWidth, "KCW")
