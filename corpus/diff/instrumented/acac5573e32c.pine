//@version=5
indicator("Bool Chain")
rsi_ = ta.rsi(close, 5)
sma_ = ta.sma(close, 5)
signal = rsi_ > 70 and close > sma_ ? 2 : rsi_ < 30 and close < sma_ ? -2 : rsi_ > 50 ? 1 : -1
var float __obs_plot0 = na
__obs_plot0 := signal
plot(signal)
