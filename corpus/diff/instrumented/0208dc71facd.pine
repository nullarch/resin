//@version=5
indicator("SimpleReward")
reward = close > open ? 1.0 : -1.0
var float totalReward = 0.0
totalReward := totalReward + reward
var float __obs_plot0 = na
__obs_plot0 := totalReward
plot(totalReward, "TotalR")
