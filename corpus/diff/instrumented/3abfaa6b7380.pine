
//@version=5
indicator("R:R")
atr = nz(ta.atr(3), 1.0)
stopDist = atr * 2.0
targetDist = atr * 4.0
rrRatio = stopDist > 0 ? targetDist / stopDist : 0.0
var float __obs_plot0 = na
__obs_plot0 := rrRatio
plot(rrRatio, "RR")
