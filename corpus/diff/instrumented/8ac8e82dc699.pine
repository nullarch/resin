//@version=5
indicator("ML Ternary Color")
fast = ta.ema(close, 3)
slow = ta.ema(close, 5)
hist = fast - slow
c = hist >= 0 ? (hist > hist[1] ? color.green : color.lime) : (hist > hist[1] ? color.orange : color.red)
var float __obs_plot0 = na
__obs_plot0 := hist
plot(hist, color=c)
