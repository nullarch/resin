//@version=5
indicator("Euler Totient Approx")
n = math.floor(close)
totient = n > 1.0 ? n * (1.0 - 1.0 / 2.0) * (1.0 - 1.0 / 3.0) : 1.0
var float __obs_plot0 = na
__obs_plot0 := totient
plot(totient, "Totient")
