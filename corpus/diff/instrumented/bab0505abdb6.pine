
//@version=5
indicator("ATR Reg")
atr = nz(ta.atr(3), 1.0)
avgATR = ta.sma(nz(ta.atr(3), 1.0), 5)
ratio = avgATR > 0 ? atr / avgATR : 1.0
highVol = ratio > 1.3
lowVol = ratio < 0.7
var float __obs_plot0 = na
__obs_plot0 := ratio
plot(ratio, "R")
