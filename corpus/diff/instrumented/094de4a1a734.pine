
//@version=5
indicator("Map Multi Key")
var m = map.new<string, float>()
map.put(m, "open", open)
map.put(m, "high", high)
map.put(m, "low", low)
map.put(m, "close", close)
spread = map.get(m, "high", 0.0) - map.get(m, "low", 0.0)
var float __obs_plot0 = na
__obs_plot0 := spread
plot(spread, "Spread")
