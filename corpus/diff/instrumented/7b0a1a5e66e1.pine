
//@version=5
indicator("POC Simple")
var float poc = 0.0
vol = volume
midPrice = (high + low) / 2.0
if vol > 1000
    poc := midPrice
else
    poc := nz(poc, midPrice)
var float __obs_plot0 = na
__obs_plot0 := poc
plot(poc, "POC")
