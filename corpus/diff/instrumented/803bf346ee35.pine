//@version=5
indicator("TradeIntensity")
priceRange = high - low
intensity = priceRange > 0.01 ? float(volume) / priceRange : 0.0
smoothed = nz(ta.sma(intensity, 3), intensity)
var float __obs_plot0 = na
__obs_plot0 := smoothed
plot(smoothed, "TI")
