//@version=5
indicator("Box Draw", overlay=true)
if barstate.islast
    box.new(bar_index - 10, high, bar_index, low, border_color=close > open ? color.green : color.red)
var float result = 0.0
result := close
var float __obs_plot0 = na
__obs_plot0 := result
plot(result)
