
//@version=5
indicator("Stack")
var float[] stack = array.new_float(0)
sma3 = ta.sma(close, 3)
if close > sma3
    array.push(stack, close)
if array.size(stack) > 5
    array.pop(stack)
depth = array.size(stack)
var float __obs_plot0 = na
__obs_plot0 := depth
plot(depth, "D")
