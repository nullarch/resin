
//@version=5
indicator("Trend Conv")
ema3 = ta.ema(close, 3)
sma5 = ta.sma(close, 5)
gap = math.abs(ema3 - sma5)
estimate = gap
iters = 0
while estimate > 0.01 and iters < 30
    estimate := estimate * 0.85
    iters := iters + 1
var float __obs_plot0 = na
__obs_plot0 := iters
plot(iters, "Its")
