//@version=5
indicator("Array Chain")
var arr = array.new_float(0)
array.clear(arr)
for i = 0 to 4
    array.push(arr, close[i])
sorted = array.copy(arr)
array.sort(sorted)
med = array.get(sorted, 2)
var float __obs_plot0 = na
__obs_plot0 := med
plot(med)
