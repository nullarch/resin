//@version=5
indicator("Rayleigh Dispersion")
short = ta.sma(close, 3)
med = ta.sma(close, 5)
dispersion = not na(short) and not na(med) ? short - med : 0.0
rsiVal = nz(ta.rsi(close, 5), 50.0)
weighted = dispersion * (rsiVal / 50.0)
var float __obs_plot0 = na
__obs_plot0 := weighted
plot(weighted)
