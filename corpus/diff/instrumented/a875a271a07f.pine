//@version=5
indicator("C300-01")
var float cumRsi = 0.0
rsiVal = nz(ta.rsi(close, 3), 50.0)
cumRsi := cumRsi + (rsiVal > 50 ? 1.0 : -1.0)
arr = array.new_float(3, 0.0)
for i = 0 to 2
    array.set(arr, i, cumRsi + i)
var float __obs_plot0 = na
__obs_plot0 := array.get(arr, 0)
plot(array.get(arr, 0))
