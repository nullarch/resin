
//@version=5
indicator("LB Filter")
sma3 = ta.sma(close, 3)
aboveCount = 0
for i = 0 to 3
    if not na(close[i]) and close[i] > sma3
        aboveCount := aboveCount + 1
filtered = aboveCount >= 3
var float __obs_plot0 = na
__obs_plot0 := filtered ? 1 : 0
plot(filtered ? 1 : 0, "F")
