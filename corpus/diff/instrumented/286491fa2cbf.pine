
//@version=5
indicator("Sort Idx")
vals = array.from(close, open, high, low)
indices = array.sort_indices(vals, "descending")
topIdx = array.size(indices) > 0 ? array.get(indices, 0) : 0
var float __obs_plot0 = na
__obs_plot0 := topIdx
plot(topIdx, "TopIdx")
