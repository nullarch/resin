
//@version=5
indicator("MinMax")
hi5 = ta.highest(high, 5)
lo5 = ta.lowest(low, 5)
rng = hi5 - lo5
scaled = rng > 0 ? (close - lo5) / rng : 0.5
var float __obs_plot0 = na
__obs_plot0 := scaled
plot(scaled, "S")
