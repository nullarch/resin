
//@version=5
indicator("BB Squeeze")
[mid, upper, lower] = ta.bb(close, 5, 2.0)
bbWidth = upper - lower
avgWidth = ta.sma(bbWidth, 5)
squeeze = avgWidth > 0 ? bbWidth / avgWidth < 0.8 : false
var float __obs_plot0 = na
__obs_plot0 := squeeze ? 1 : 0
plot(squeeze ? 1 : 0, "Sq")
