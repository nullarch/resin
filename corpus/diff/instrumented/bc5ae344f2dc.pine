//@version=5
indicator("C300-06")
arr = array.new_float(5, 0.0)
for i = 0 to 4
    array.set(arr, i, close + i * 0.5)
array.sort(arr)
midIdx = math.floor(array.size(arr) / 2)
midVal = array.get(arr, midIdx)
var float __obs_plot0 = na
__obs_plot0 := midVal
plot(midVal)
