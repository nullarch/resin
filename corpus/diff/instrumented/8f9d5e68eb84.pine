
//@version=5
indicator("Round Num")
roundLevel = math.round(close / 10) * 10
dist = close - roundLevel
nearRound = math.abs(dist) < 1
var float __obs_plot0 = na
__obs_plot0 := nearRound ? 1 : 0
plot(nearRound ? 1 : 0, "NR")
