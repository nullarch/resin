//@version=5
indicator("test")
var float[] highs = array.new_float(0)
if close > open
    array.push(highs, high)
    if array.size(highs) > 5
        array.shift(highs)
var float __obs_plot0 = na
__obs_plot0 := array.size(highs)
plot(array.size(highs))
