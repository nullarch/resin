//@version=5
indicator("Betti-0")
float dist = math.abs(close - open)
float epsilon = 3.0
var int components = 0
if dist > epsilon
    components := components + 1
else
    components := math.max(components - 1, 0)
var float __obs_plot0 = na
__obs_plot0 := components
plot(components, "Betti0")
