//@version=5
indicator("Wait Time")
float waitT = volume > 0 ? (high - low) / volume * 10000.0 : 0.0
float avgWait = ta.sma(waitT, 5)
var float __obs_plot0 = na
__obs_plot0 := nz(avgWait, 0.0)
plot(nz(avgWait, 0.0), "AvgWait")
