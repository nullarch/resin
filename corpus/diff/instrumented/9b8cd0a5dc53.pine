
//@version=5
indicator("Vol Ratio")
avgVol = ta.sma(volume, 5)
ratio = avgVol > 0 ? volume / avgVol : 1.0
highVol = ratio > 2.0
lowVol = ratio < 0.5
var float __obs_plot0 = na
__obs_plot0 := ratio
plot(ratio, "VR")
