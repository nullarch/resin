
//@version=5
indicator("Tern Args")
rsi = nz(ta.rsi(close, 3), 50.0)
period = rsi > 60 ? 3 : rsi < 40 ? 5 : 4
ma = ta.sma(close, period)
src = close > ma ? close : ma
smoothed = ta.ema(src, 3)
var float __obs_plot0 = na
__obs_plot0 := smoothed
plot(smoothed, "Smooth")
