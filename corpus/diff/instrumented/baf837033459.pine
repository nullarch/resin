
//@version=5
indicator("Dual TF")
fast = ta.ema(close, 3)
slow = ta.sma(close, 5)
fastTrend = close > fast
slowTrend = close > slow
aligned = fastTrend and slowTrend
var float __obs_plot0 = na
__obs_plot0 := aligned ? 1 : 0
plot(aligned ? 1 : 0, "Align")
