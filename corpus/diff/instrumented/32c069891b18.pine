
//@version=5
indicator("FWA Combo")
var float[] data = array.new_float(0)
for i = 0 to 3
    c = close[i]
    if not na(c)
        array.push(data, c)
while array.size(data) > 6
    array.shift(data)
avg = array.avg(data)
var float __obs_plot0 = na
__obs_plot0 := avg
plot(avg, "Avg")
