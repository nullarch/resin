
//@version=5
indicator("Vol Map")
var vm = map.new<string, float>()
map.put(vm, "vol", volume)
map.put(vm, "avg", ta.sma(volume, 5))
map.put(vm, "price", close)
total = 0.0
for [k, v] in vm
    total := total + v
var float __obs_plot0 = na
__obs_plot0 := total
plot(total, "Tot")
