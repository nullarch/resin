//@version=5
indicator("Brownian Path")
var float cumRet = 0.0
float prevC = not na(close[1]) ? close[1] : close
float logRet = prevC > 0 ? math.log(close / prevC) : 0.0
cumRet := cumRet + logRet
var float __obs_plot0 = na
__obs_plot0 := cumRet
plot(cumRet, "CumRet")
