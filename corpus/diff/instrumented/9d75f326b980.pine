
//@version=5
indicator("VW Close")
var float sumPV = 0.0
var float sumV = 0.0
sumPV := sumPV + close * volume
sumV := sumV + volume
vwClose = sumV > 0 ? sumPV / sumV : close
var float __obs_plot0 = na
__obs_plot0 := vwClose
plot(vwClose, "VWC")
