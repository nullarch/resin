//@version=5
indicator("KalFor08")
var float est = 100.0
gain = 0.25
est := est + gain * (close - est)
sumDev = 0.0
for i = 0 to 4
    dev = math.abs(est - (close + float(i) * 0.1))
    sumDev := sumDev + dev
avgDev = sumDev / 5.0
var float __obs_plot0 = na
__obs_plot0 := avgDev
plot(avgDev, "ad")
