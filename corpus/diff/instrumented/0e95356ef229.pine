
//@version=5
indicator("Cumul")
cumClose = ta.cum(close)
cumVol = ta.cum(volume)
vwap = cumVol > 0 ? ta.cum(close * volume) / cumVol : close
var float __obs_plot0 = na
__obs_plot0 := vwap
plot(vwap, "VWAP")
