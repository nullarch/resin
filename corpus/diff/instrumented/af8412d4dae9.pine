
//@version=5
indicator("Indicator Registry")
var m = map.new<string, float>()
map.put(m, "sma3", ta.sma(close, 3))
map.put(m, "ema5", ta.ema(close, 5))
map.put(m, "rsi3", ta.rsi(close, 3))
map.put(m, "atr3", ta.atr(3))
sz = map.size(m)
vals = map.values(m)
avgIndicator = array.avg(vals)
var float __obs_plot0 = na
__obs_plot0 := avgIndicator
plot(avgIndicator, "AvgInd")
