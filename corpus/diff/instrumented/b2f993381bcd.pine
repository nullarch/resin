//@version=5
indicator("NS For Bounds")
var arr = array.from(10.0, 20.0, 30.0, 40.0, 50.0)
float total = 0.0
for i = 0 to math.min(array.size(arr), 3) - 1
    total := total + array.get(arr, i)
// 10+20+30 = 60
var float last_total = 0.0
last_total := total
var float __obs_plot0 = na
__obs_plot0 := close
plot(close)
