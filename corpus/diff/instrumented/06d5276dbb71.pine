
//@version=5
indicator("Dual MA")
fast = ta.sma(close, 3)
slow = ta.sma(close, 5)
bullish = fast > slow
bearish = fast < slow
signal = bullish ? 1 : bearish ? -1 : 0
var float __obs_plot0 = na
__obs_plot0 := signal
plot(signal, "Sig")
