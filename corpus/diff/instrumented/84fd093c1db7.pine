
//@version=5
indicator("Math Chain")
val = close - open
absVal = math.abs(val)
logVal = absVal > 0 ? math.log(1 + absVal) : 0.0
sqrtVal = math.sqrt(absVal)
final = val > 0 ? logVal + sqrtVal : -(logVal + sqrtVal)
var float __obs_plot0 = na
__obs_plot0 := final
plot(final, "Final")
