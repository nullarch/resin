//@version=5
indicator("Series Forward")
// close is a Series: push each bar, get(0)=current, get(1)=previous
var float curr = 0.0
var float prev = 0.0
curr := close
prev := nz(close[1], 0.0)
var float result = 0.0
result := curr - prev
var float __obs_plot0 = na
__obs_plot0 := result
plot(result)
