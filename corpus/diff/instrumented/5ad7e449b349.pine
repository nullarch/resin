//@version=5
indicator("test")
type Inner
    float val = 0.0

type Outer
    Inner inner = na

o = Outer.new(Inner.new(close))
var float __obs_plot0 = na
__obs_plot0 := o.inner.val
plot(o.inner.val)
