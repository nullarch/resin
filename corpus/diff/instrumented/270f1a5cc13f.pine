
//@version=5
indicator("Beta")
ret = close[1] > 0 ? (close - close[1]) / close[1] : 0.0
avgRet = ta.sma(ret, 5)
retDev = ret - avgRet
absDev = math.abs(retDev)
avgDev = ta.sma(absDev, 5)
beta = avgDev > 0 ? retDev / avgDev : 1.0
var float __obs_plot0 = na
__obs_plot0 := beta
plot(beta, "Beta")
