
//@version=5
indicator("Hysteresis")
var bool active = false
upperThresh = 107.0
lowerThresh = 104.0

if not active and close > upperThresh
    active := true
if active and close < lowerThresh
    active := false

var float __obs_plot0 = na
__obs_plot0 := active ? 1 : 0
plot(active ? 1 : 0, "A")
