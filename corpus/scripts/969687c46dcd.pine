//@version=5
indicator("Albedo")
incoming = high - low
reflected = math.abs(close - open)
albedo = incoming > 0 ? reflected / incoming : 0.0
var float avgAlbedo = 0.0
var int cnt = 0
cnt := cnt + 1
avgAlbedo := avgAlbedo + (albedo - avgAlbedo) / cnt
