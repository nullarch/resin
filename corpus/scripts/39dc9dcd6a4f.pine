
//@version=5
indicator("Level Str")
levelStrength(float price, float level, float atr) =>
    dist = math.abs(price - level)
    atr > 0 ? 1.0 - math.min(dist / atr, 1.0) : 0.0

atr = nz(ta.atr(3), 1.0)
hi5 = ta.highest(high, 5)
str = levelStrength(close, hi5, atr)
plot(str, "LS")
