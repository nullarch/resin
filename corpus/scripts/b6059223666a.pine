//@version=5
indicator("Persistence Birth")
float emaVal = ta.ema(close, 3)
var float birthBar = -1.0
float diff = close - emaVal
if diff > 0 and birthBar < 0
    birthBar := bar_index * 1.0
plot(birthBar, "Birth")
