
//@version=5
indicator("Sliding Z")
var float[] window = array.new_float(0)
array.push(window, close)
if array.size(window) > 5
    array.shift(window)
avg = array.avg(window)
sd = array.stdev(window)
zscore = sd > 0 ? (close - avg) / sd : 0.0
plot(zscore, "Z")
