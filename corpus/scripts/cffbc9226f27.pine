
//@version=5
indicator("UDT Copy")
type Snapshot
    float price = 0.0
    int idx = 0

var Snapshot best = Snapshot.new(0.0, 0)
current = Snapshot.new(close, bar_index)
if current.price > best.price
    best := Snapshot.copy(current)
plot(best.price, "Best Price")
