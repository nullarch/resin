
//@version=5
indicator("NA Safe Arith")
a = close[8]
b = close[1]
safeA = nz(a, close)
safeB = nz(b, close)
sum = safeA + safeB
diff = safeA - safeB
product = safeA * safeB / close
plot(sum, "Sum")
plot(diff, "Diff")
