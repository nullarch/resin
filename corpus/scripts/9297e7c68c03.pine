//@version=5
strategy("DSP Momentum")
fast = ta.ema(close, 3)
slow = ta.ema(close, 7)
momentum = fast - slow
if momentum > 0
    strategy.entry("Long", strategy.long)
if momentum < 0
    strategy.close("Long")
