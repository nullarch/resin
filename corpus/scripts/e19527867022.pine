
//@version=5
indicator("Candle Scan")
bullCount = 0
for i = 0 to 4
    if not na(close[i]) and not na(open[i])
        if close[i] > open[i]
            bullCount := bullCount + 1
bullPct = bullCount / 5.0 * 100
plot(bullPct, "BP")
