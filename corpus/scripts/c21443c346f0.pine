
//@version=5
indicator("Tuple UDF")
calcBands(src, len, mult) =>
    basis = ta.sma(src, len)
    dev = ta.stdev(src, len) * mult
    [basis, basis + dev, basis - dev]

[mid, upper, lower] = calcBands(close, 5, 2.0)
plot(mid, "Mid")
plot(upper, "Upper")
plot(lower, "Lower")
