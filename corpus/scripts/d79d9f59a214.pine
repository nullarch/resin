
//@version=5
indicator("CCI Mom")
cciVal = ta.cci(close, 5)
overbought = cciVal > 100
oversold = cciVal < -100
var int obCount = 0
var int osCount = 0
if overbought
    obCount := obCount + 1
if oversold
    osCount := osCount + 1
plot(cciVal, "CCI")
