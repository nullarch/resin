
//@version=5
indicator("Regime Dur")
var int regime = 0
var int duration = 0
sma3 = ta.sma(close, 3)
newRegime = close > sma3 ? 1 : -1
if newRegime == regime
    duration := duration + 1
else
    regime := newRegime
    duration := 1
plot(duration, "Duration")
plot(regime, "Regime")
