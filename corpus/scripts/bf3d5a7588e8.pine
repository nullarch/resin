//@version=5
indicator("Focal Mechanism")
vertical = close - open
horizontal = high - low
ratio = horizontal > 0 ? math.abs(vertical) / horizontal : 0.0
mechanism = 0
if ratio > 0.7
    mechanism := 1
else if ratio > 0.3
    mechanism := 2
else
    mechanism := 3
plot(mechanism)
