
//@version=5
strategy("Stat Strat", overlay=true)
sma5 = ta.sma(close, 5)
sd = ta.stdev(close, 5)
z = sd > 0 ? (close - sma5) / sd : 0.0
if z < -1.5
    strategy.entry("Long", strategy.long)
if z > 1.0
    strategy.close("Long")
plot(sma5, "MA")
