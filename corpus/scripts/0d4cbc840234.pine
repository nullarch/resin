
//@version=5
indicator("Var Ind")
var float ema = 0.0
var int bars = 0
bars := bars + 1
alpha = 2.0 / (3 + 1)
if bars == 1
    ema := close
else
    ema := alpha * close + (1 - alpha) * ema
diff = close - ema
plot(diff, "D")
