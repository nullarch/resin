
//@version=5
strategy("Mean Rev", overlay=true)
sma5 = ta.sma(close, 5)
stdev5 = ta.stdev(close, 5)
z = stdev5 > 0 ? (close - sma5) / stdev5 : 0.0
if z < -1.5
    strategy.entry("Long", strategy.long)
if z > 0.5
    strategy.close("Long")
plot(sma5, "MA")
