
//@version=5
indicator("Pat Count")
var int bullPatterns = 0
var int bearPatterns = 0
bullBar = close > open
bearBar = close < open
if bullBar and close[1] < open[1]
    bullPatterns := bullPatterns + 1
if bearBar and close[1] > open[1]
    bearPatterns := bearPatterns + 1
plot(bullPatterns - bearPatterns, "Net")
