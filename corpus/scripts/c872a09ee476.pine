
//@version=5
indicator("MA Env")
sma5 = ta.sma(close, 5)
pct = 2.0
upper = sma5 * (1 + pct / 100)
lower = sma5 * (1 - pct / 100)
aboveUpper = close > upper
belowLower = close < lower
plot(aboveUpper ? 1 : belowLower ? -1 : 0, "Env")
