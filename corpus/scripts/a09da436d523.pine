//@version=5
indicator("Array fill clear")
arr = array.new_float(5, 0.0)
array.fill(arr, close)
sz1 = array.size(arr)
sm1 = array.sum(arr)
array.clear(arr)
sz2 = array.size(arr)
array.push(arr, 1.0)
array.push(arr, 2.0)
