//@version=5
strategy("EMA Cross")
var float result = 0.0
ema3 = ta.ema(close, 3)
ema5 = ta.ema(close, 5)
if ta.crossover(ema3, ema5)
    strategy.entry("Long", strategy.long)
if ta.crossunder(ema3, ema5)
    strategy.close("Long")
result := close
