//@version=5
indicator("NS-13 Poiseuille")
var float pGrad = 0.0
if not na(close[1])
    pGrad := (close - close[1]) * volume / 1000.0
atrVal = nz(ta.atr(3), 1.0)
viscosity = math.max(atrVal * 0.5, 0.01)
flowRate = pGrad / viscosity
plot(flowRate)
