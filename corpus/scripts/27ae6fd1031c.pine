//@version=5
indicator("RSI Div")
var float result = 0.0
rsiVal = ta.rsi(close, 5)
priceHigher = close > close[1]
rsiLower = rsiVal < rsiVal[1]
bearDiv = priceHigher and rsiLower
result := bearDiv ? 1.0 : 0.0
