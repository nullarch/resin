//@version=5
indicator("Orbital Eccentricity")
apoapsis = high
periapsis = low
sumAP = apoapsis + periapsis
ecc = sumAP > 0 ? (apoapsis - periapsis) / sumAP : 0.0
var float avgEcc = 0.0
var int cnt = 0
cnt := cnt + 1
avgEcc := avgEcc + (ecc - avgEcc) / cnt
