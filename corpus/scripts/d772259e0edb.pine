
//@version=5
indicator("Cond String")
rsi = ta.rsi(close, 3)
sma3 = ta.sma(close, 3)
parts = "Signals:"
if close > sma3
    parts := parts + " [TREND+]"
if rsi > 60
    parts := parts + " [MOM+]"
if rsi < 40
    parts := parts + " [MOM-]"
len = str.length(parts)
plot(len, "Len")
