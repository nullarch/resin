
//@version=5
indicator("Range Exp")
var float avgRange = 0.0
var int count = 0
rng = high - low
count := count + 1
avgRange := avgRange + (rng - avgRange) / count
expansion = avgRange > 0 ? rng / avgRange : 1.0
plot(expansion, "Exp")
