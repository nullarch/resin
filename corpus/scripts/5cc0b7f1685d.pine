//@version=5
indicator("Markov Automaton")
var int state = 0
var int transitions = 0
trigger = close > open
if state == 0 and trigger
    state := 1
    transitions := transitions + 1
else if state == 1 and not trigger
    state := 0
    transitions := transitions + 1
