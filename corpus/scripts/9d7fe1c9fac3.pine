//@version=5
indicator("Map Only")
m = map.new<string, float>()
map.put(m, "a", 10.0)
map.put(m, "b", 20.0)
var float result = 0.0
if map.contains(m, "a")
    result := map.get(m, "a") + map.get(m, "b")
plot(result)
