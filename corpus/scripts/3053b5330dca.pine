
//@version=5
indicator("Vol Scan")
aboveAvg = 0
avgVol = ta.sma(volume, 5)
for i = 0 to 4
    if not na(volume[i])
        if volume[i] > avgVol
            aboveAvg := aboveAvg + 1
volStr = aboveAvg / 5.0 * 100
plot(volStr, "VS")
