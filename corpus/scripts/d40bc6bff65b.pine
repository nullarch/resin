
//@version=5
indicator("MACross")
fast = ta.ema(close, 3)
slow = ta.sma(close, 5)
var int signal = 0
if fast > slow
    signal := 1
else
    signal := -1
var int crossCount = 0
prevSig = nz(signal[1], 0)
if signal != prevSig and prevSig != 0
    crossCount := crossCount + 1
plot(crossCount, "XC")
