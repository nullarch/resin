//@version=5
indicator("MapTA")
var m = map.new<string, float>()
smaVal = ta.sma(close, 3)
emaVal = ta.ema(close, 3)
if not na(smaVal)
    map.put(m, "sma", smaVal)
if not na(emaVal)
    map.put(m, "ema", emaVal)
var int sz = 0
sz := map.size(m)
