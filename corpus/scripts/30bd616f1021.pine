
//@version=5
indicator("Osc Div")
rsi = nz(ta.rsi(close, 3), 50.0)
priceLower = false
rsiHigher = false
for i = 1 to 3
    if not na(close[i]) and close < close[i]
        priceLower := true
    if not na(rsi[i]) and rsi > rsi[i]
        rsiHigher := true
bullDiv = priceLower and rsiHigher
plot(bullDiv ? 1 : 0, "Div")
