//@version=5
strategy("Full Strategy")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
longCond = ta.crossover(sma3, sma5)
shortCond = ta.crossunder(sma3, sma5)
if longCond
    strategy.entry("Long", strategy.long)
if shortCond
    strategy.close("Long")
