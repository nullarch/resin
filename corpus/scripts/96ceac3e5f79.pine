//@version=5
strategy("Basic Strategy")
longCondition = ta.crossover(ta.sma(close, 3), ta.sma(close, 5))
if longCondition
    strategy.entry("Long", strategy.long)
shortCondition = ta.crossunder(ta.sma(close, 3), ta.sma(close, 5))
if shortCondition
    strategy.close("Long")
