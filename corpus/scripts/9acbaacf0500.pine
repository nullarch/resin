//@version=5
indicator("MTF_Pattern")
var float result = 0
fast = ta.ema(close, 3)
slow = ta.ema(close, 5)
if not na(fast) and not na(slow)
    trend = fast > slow ? 1 : -1
    rsiVal = ta.rsi(close, 5)
    if not na(rsiVal)
        result := trend * rsiVal
