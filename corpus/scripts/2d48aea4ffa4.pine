
//@version=5
indicator("Vol Scan")
highVolCount = 0
avgVol = ta.sma(volume, 5)
for i = 0 to 4
    v = volume[i]
    if not na(v) and v > avgVol * 1.5
        highVolCount := highVolCount + 1
volTrend = highVolCount > 2 ? 1 : 0
plot(volTrend, "VT")
