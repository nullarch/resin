
//@version=5
indicator("Ind Mode")
atr = nz(ta.atr(3), 1.0)
avgATR = ta.sma(nz(ta.atr(3), 1.0), 5)
ratio = avgATR > 0 ? atr / avgATR : 1.0
rsi = nz(ta.rsi(close, 3), 50.0)
result = switch
    ratio > 1.5 => rsi * 0.5
    ratio > 1.0 => rsi * 0.8
    => rsi
plot(result, "R")
