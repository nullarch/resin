//@version=5
indicator("Sufficient Stats")
var float sumX = 0.0
var float sumX2 = 0.0
var int cnt = 0
cnt := cnt + 1
sumX := sumX + close
sumX2 := sumX2 + close * close
meanEst = cnt > 0 ? sumX / cnt : 0.0
varEst = cnt > 1 ? (sumX2 / cnt) - (meanEst * meanEst) : 0.0
