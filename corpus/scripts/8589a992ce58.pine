//@version=5
indicator("Triple Ternary ML")
x = close > open ?
    close > close[1] ?
        close > high[1] ? 3 : 2 :
        1 :
    close < open ?
        close < close[1] ? -2 : -1 :
    0
plot(x)
