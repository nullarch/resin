//@version=5
indicator("MethodRun")
var float result = 0
type Point
    float x = 0.0
    float y = 0.0
method magnitude(Point this) =>
    math.sqrt(this.x * this.x + this.y * this.y)
pt = Point.new(x=3.0, y=4.0)
result := pt.magnitude()
