//@version=5
indicator("test")
var m = map.new<string, float>()
if barstate.isfirst
    map.put(m, "a", 1.0)
    map.put(m, "b", 2.0)
    map.put(m, "c", 3.0)
var float total = 0.0
total := 0.0
for [key, val] in m
    total := total + val
plot(total)
