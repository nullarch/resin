
//@version=5
indicator("Time Anal")
totalRange = 0.0
for i = 0 to 4
    if not na(high[i]) and not na(low[i])
        totalRange := totalRange + (high[i] - low[i])
avgRange = totalRange / 5
plot(avgRange, "AR")
