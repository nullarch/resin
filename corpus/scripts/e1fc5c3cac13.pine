
//@version=5
indicator("TernaryFunc")
smaVal = nz(ta.sma(close, 3), close)
emaVal = nz(ta.ema(close, 3), close)
best = close > smaVal ? math.max(smaVal, emaVal) : math.min(smaVal, emaVal)
diff = math.abs(close - best)
plot(diff, "D")
