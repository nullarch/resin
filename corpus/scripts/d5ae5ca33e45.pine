
//@version=5
strategy("RoughnessStrat")
length = 5
rangeHL = high - low
avgRange = ta.sma(rangeHL, length)
roughness = avgRange > 0 ? rangeHL / avgRange : 1.0
if roughness > 1.2
    strategy.entry("Rough", strategy.long)
if roughness < 0.8
    strategy.close("Rough")
plot(roughness, "R")
