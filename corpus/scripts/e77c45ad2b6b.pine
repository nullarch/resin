//@version=5
indicator("EdgePersist")
var int edgeLife = 0
thresh = 4.0
edgeExists = math.abs(close - open) < thresh ? 1 : 0
if edgeExists == 1
    edgeLife := edgeLife + 1
else
    edgeLife := 0
persistence = float(edgeLife)
plot(persistence, "EdgeLife")
