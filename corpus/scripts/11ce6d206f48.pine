//@version=5
indicator("Compound Member")
type Stats
    float total = 0.0
    int count = 0

var s = Stats.new()
s.total += close
s.count += 1

var float avg_ = 0.0
avg_ := s.total / s.count
plot(close)
