//@version=5
indicator("Array Slice")
arr = array.new<float>(0)
array.push(arr, 10.0)
array.push(arr, 20.0)
array.push(arr, 30.0)
array.push(arr, 40.0)
sliced = array.slice(arr, 1, 3)
var float result = 0.0
result := array.sum(sliced)
plot(result)
