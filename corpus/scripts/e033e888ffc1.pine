
//@version=5
indicator("Candle Type")
candleScore(float o, float h, float l, float c) =>
    body = math.abs(c - o)
    range_ = h - l
    ratio = range_ > 0 ? body / range_ : 0.0
    ratio > 0.7 ? 2.0 : ratio > 0.3 ? 1.0 : 0.0

score = candleScore(open, high, low, close)
plot(score, "Score")
