
//@version=5
indicator("Vol Dist")
var float[] vols = array.new_float(0)
array.push(vols, volume)
if array.size(vols) > 8
    array.shift(vols)
avgVol = array.avg(vols)
sdVol = array.stdev(vols)
maxVol = array.max(vols)
relVol = avgVol > 0 ? volume / avgVol : 1.0
plot(relVol, "RelVol")
