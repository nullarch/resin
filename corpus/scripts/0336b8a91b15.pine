
//@version=5
indicator("BB %B")
[mid, upper, lower] = ta.bb(close, 5, 2.0)
bandwidth = upper - lower
pctB = bandwidth != 0 ? (close - lower) / bandwidth : 0.5
extreme = pctB > 1.0 or pctB < 0.0
plot(pctB, "PctB")
plot(extreme ? 1 : 0, "Extreme")
