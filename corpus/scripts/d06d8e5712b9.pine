//@version=5
indicator("Induction Variable")
float base = close * 0.01
float accum = 0.0
for i = 0 to 5
    accum := accum + base
float multiplied = close * 0.01 * 6.0
float diff = math.abs(accum - multiplied)
