
//@version=5
strategy("Breakout", overlay=true)
hi5 = ta.highest(high, 5)
lo5 = ta.lowest(low, 5)
rsi = nz(ta.rsi(close, 3), 50.0)
if close >= hi5 and rsi > 50
    strategy.entry("Long", strategy.long)
if close <= lo5 or rsi < 40
    strategy.close("Long")
plot(hi5, "R")
