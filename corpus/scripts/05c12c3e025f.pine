
//@version=5
indicator("Trend Wt")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
spread = sma3 - sma5
weight = spread > 1.0 ? 2.0 : spread > 0 ? 1.0 : spread > -1.0 ? 0.5 : 0.25
rsi = nz(ta.rsi(close, 3), 50.0)
signal = (rsi - 50) / 50 * weight
plot(signal, "Sig")
