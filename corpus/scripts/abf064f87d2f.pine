//@version=5
indicator("Switch ML Cond")
signal = switch
    close > open and
        volume > 1200 => 2
    close > open => 1
    close < open and
        volume > 1200 => -2
    close < open => -1
    => 0
plot(signal)
