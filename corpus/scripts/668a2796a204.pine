//@version=5
indicator("Sine01")
var int barIdx = 0
barIdx := barIdx + 1
period = 5.0
phase = 2.0 * math.pi * float(barIdx) / period
sineVal = math.sin(phase)
cosVal = math.cos(phase)
plot(sineVal, "sin")
plot(cosVal, "cos")
