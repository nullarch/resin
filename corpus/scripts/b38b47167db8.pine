
//@version=5
indicator("Run Stats")
var float sumX = 0.0
var float sumX2 = 0.0
var int n = 0
n := n + 1
sumX := sumX + close
sumX2 := sumX2 + close * close
mean = sumX / n
variance = n > 1 ? (sumX2 / n - mean * mean) : 0.0
sd = math.sqrt(math.abs(variance))
plot(mean, "Mean")
plot(sd, "SD")
