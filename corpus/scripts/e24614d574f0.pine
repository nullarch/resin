//@version=5
indicator("Var max min")
var float allTimeHigh = 0.0
var float allTimeLow = 999999.0
var float avgRange = 0.0
if close > allTimeHigh
    allTimeHigh := close
if close < allTimeLow
    allTimeLow := close
avgRange := (allTimeHigh - allTimeLow) / 2.0
