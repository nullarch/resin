
//@version=5
indicator("Wt Conf")
rsi = nz(ta.rsi(close, 3), 50.0)
sma3 = ta.sma(close, 3)
rsiWt = rsi > 55 ? 2.0 : rsi < 45 ? -2.0 : 0.0
maWt = close > sma3 ? 1.5 : -1.5
volWt = volume > ta.sma(volume, 3) ? 1.0 : 0.0
total = rsiWt + maWt + volWt
plot(total, "Tot")
