
//@version=5
indicator("Level Scan")
hi5 = ta.highest(high, 5)
lo5 = ta.lowest(low, 5)
nearResist = 0
for i = 0 to 4
    if not na(high[i])
        if high[i] >= hi5 * 0.99
            nearResist := nearResist + 1
plot(nearResist, "NR")
