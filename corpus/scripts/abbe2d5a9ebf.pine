
//@version=5
indicator("Pos Size")
calcPositionSize(float capital, float riskPct, float stopDist) =>
    riskAmount = capital * riskPct / 100
    stopDist > 0 ? riskAmount / stopDist : 0.0

atr = ta.atr(3)
size = calcPositionSize(10000.0, 2.0, atr * 2)
plot(size, "Size")
