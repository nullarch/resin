//@version=5
indicator("Enum State")
enum Dir
    Up = 1
    Down = -1
    Flat = 0
var Dir d = Dir.Flat
if close > close[1]
    d := Dir.Up
else if close < close[1]
    d := Dir.Down
else
    d := Dir.Flat
var float result = 0.0
result := int(d)
