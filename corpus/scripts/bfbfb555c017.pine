//@version=5
indicator("Sharding")
ll = ta.lowest(low, 5)
hh = ta.highest(high, 5)
range_size = not na(hh) and not na(ll) and hh > ll ? (hh - ll) : 1.0
shard_pos = not na(ll) ? (close - ll) / range_size : 0.5
shard_id = math.floor(shard_pos * 3.0)
