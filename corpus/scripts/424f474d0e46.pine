//@version=5
indicator("C300-16")
buckets = array.new_float(5, 0.0)
range_ = high - low
if range_ > 0
    idx = math.floor((close - low) / range_ * 4.0)
    idx := math.min(idx, 4)
    idx := math.max(idx, 0)
    prev = array.get(buckets, int(idx))
    array.set(buckets, int(idx), prev + volume)
plot(array.get(buckets, 2))
