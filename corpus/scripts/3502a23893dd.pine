
//@version=5
indicator("Reg Wt")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
rsi = nz(ta.rsi(close, 3), 50.0)
aligned = sma3 > sma5
trendWt = aligned ? 1.5 : 0.75
momWt = rsi > 55 or rsi < 45 ? 1.5 : 0.8
signal = (close > sma3 ? 1 : -1) * trendWt + (rsi - 50) / 50 * momWt
plot(signal, "Sig")
