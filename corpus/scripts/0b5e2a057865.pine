
//@version=5
indicator("WhileIfCont")
count = 0
idx = 0
while idx < 8
    idx := idx + 1
    if idx % 2 == 0
        continue
    if idx > 6
        break
    count := count + 1
plot(float(count), "C")
