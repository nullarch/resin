
//@version=5
indicator("Str Chain")
base = "Hello World Test"
lower = str.lower(base)
upper = str.upper(base)
sub = str.substring(base, 0, 5)
replaced = str.replace_all(base, " ", "_")
hasWorld = str.contains(replaced, "World")
plot(hasWorld ? 1 : 0, "Has")
