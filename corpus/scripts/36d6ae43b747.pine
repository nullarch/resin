
//@version=5
indicator("ADX Str")
atr = ta.atr(3)
avgATR = ta.sma(atr, 5)
trendStr = avgATR > 0 ? atr / avgATR * 25 : 0.0
strongTrend = trendStr > 25
weakTrend = trendStr < 15
plot(trendStr, "TS")
plot(strongTrend ? 1 : 0, "Strong")
