
//@version=5
indicator("UDTRegState")
type RegimeState
    float rsiVal = 50.0
    float atrVal = 1.0
    int classification = 0

rsi = nz(ta.rsi(close, 3), 50.0)
atr = nz(ta.atr(3), 1.0)
cls = rsi > 60 ? 1 : rsi < 40 ? -1 : 0
rs = RegimeState.new(rsi, atr, cls)
plot(rs.classification, "Cls")
