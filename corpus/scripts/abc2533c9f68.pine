//@version=5
indicator("BetweenProxy")
a = close
b = open
c = (high + low) / 2.0
distAB = math.abs(a - b)
distBC = math.abs(b - c)
distAC = math.abs(a - c)
bOnPath = distAB + distBC < distAC * 1.2 ? 1.0 : 0.0
betweennessB = bOnPath
plot(betweennessB, "BetB")
plot(distAC, "DistAC")
