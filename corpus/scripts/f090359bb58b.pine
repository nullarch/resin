
//@version=5
strategy("BB Bounce", overlay=true)
[mid, upper, lower] = ta.bb(close, 5, 2.0)
if close < lower
    strategy.entry("Long", strategy.long)
if close > mid
    strategy.close("Long")
plot(mid, "Mid")
plot(lower, "Lower")
