
//@version=5
indicator("Array Full")
var float[] q = array.new_float(0)
array.push(q, close)
array.unshift(q, open)
if array.size(q) > 6
    array.pop(q)
    array.shift(q)
sz = array.size(q)
avg = array.avg(q)
plot(sz, "Size")
plot(avg, "Avg")
