//@version=5
indicator("MultiVar")
var int count = 0
var float sum = 0.0
var string label = "start"
var bool found = false
count := count + 1
sum := sum + close
if close > 110
    found := true
    label := "found"
