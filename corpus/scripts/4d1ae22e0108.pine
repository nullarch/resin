
//@version=5
indicator("TrendStr")
trend = nz(ta.sma(close, 3), close)
pctDev = trend > 0 ? math.abs(close - trend) / trend * 100.0 : 0.0
label = switch
    pctDev > 2.0 => 3.0
    pctDev > 1.0 => 2.0
    pctDev > 0.5 => 1.0
    => 0.0
plot(label, "TS")
