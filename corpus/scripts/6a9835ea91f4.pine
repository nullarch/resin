//@version=5
indicator("MRTrend")
s2 = nz(ta.sma(close, 2), close)
s3 = nz(ta.sma(close, 3), close)
s5 = nz(ta.sma(close, 5), close)
allUp = close > s2 and s2 > s3 and s3 > s5 ? 1.0 : 0.0
allDown = close < s2 and s2 < s3 and s3 < s5 ? -1.0 : 0.0
trend = allUp + allDown
plot(trend, "MRT")
