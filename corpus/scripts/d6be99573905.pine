//@version=5
indicator("Empirical CDF")
var int rank = 0
float smaVal = nz(ta.sma(close, 5), close)
if close <= smaVal
    rank := rank + 1
var int total = 0
total := total + 1
float cdfVal = total > 0 ? rank / total : 0.0
plot(cdfVal, "CDF")
