
//@version=5
indicator("KC Width")
basis = ta.ema(close, 5)
atr = ta.atr(3)
upper = basis + atr * 1.5
lower = basis - atr * 1.5
kcWidth = basis > 0 ? (upper - lower) / basis * 100 : 0.0
plot(kcWidth, "KCW")
