//@version=5
indicator("FE10")
var int signals = 0
cciVal = nz(ta.cci(close, 5), 0.0)
roundedCci = math.round(cciVal)
zeroLine = 0.0
if cciVal > 100
    signals := signals + 1
else
    if cciVal < -100
        signals := signals - 1
plot(roundedCci, title="cci")
