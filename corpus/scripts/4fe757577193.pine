//@version=5
indicator("Map keys values")
var m = map.new<string, float>()
map.put(m, "a", 1.0)
map.put(m, "b", 2.0)
map.put(m, "c", 3.0)
keys = map.keys(m)
vals = map.values(m)
keysSz = array.size(keys)
valsSz = array.size(vals)
