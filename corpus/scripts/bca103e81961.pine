
//@version=5
indicator("OrderImbalance")
buyVol = close > open ? volume : volume * 0.5
sellVol = close < open ? volume : volume * 0.5
imbalance = (buyVol - sellVol) / (buyVol + sellVol + 0.001)
plot(imbalance, "OI")
