
//@version=5
indicator("Str Split")
tags = "BUY,TREND,MOMENTUM"
hasBuy = str.contains(tags, "BUY")
hasTrend = str.contains(tags, "TREND")
tagCount = (hasBuy ? 1 : 0) + (hasTrend ? 1 : 0)
totalLen = str.length(tags)
plot(tagCount, "Tags")
plot(totalLen, "Len")
