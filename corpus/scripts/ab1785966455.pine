
//@version=5
indicator("Deque")
var float[] dq = array.new_float(0)
if close > open
    array.push(dq, close)
else
    array.unshift(dq, close)
if array.size(dq) > 6
    if close > open
        array.shift(dq)
    else
        array.pop(dq)
plot(array.size(dq), "Size")
plot(array.avg(dq), "Avg")
