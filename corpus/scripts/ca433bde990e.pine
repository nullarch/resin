
//@version=5
indicator("Cross Cnt")
var int crossCount = 0
ema3 = ta.ema(close, 3)
sma5 = ta.sma(close, 5)
if ta.crossover(ema3, sma5) or ta.crossunder(ema3, sma5)
    crossCount := crossCount + 1
plot(crossCount, "Cnt")
