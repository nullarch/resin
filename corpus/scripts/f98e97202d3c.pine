
//@version=5
indicator("Encode State")
rsi = ta.rsi(close, 3)
sma3 = ta.sma(close, 3)
t = close > sma3 ? "1" : "0"
m = rsi > 50 ? "1" : "0"
v = close > open ? "1" : "0"
state = t + m + v
allBull = state == "111"
plot(allBull ? 1 : 0, "AllBull")
plot(str.length(state), "Len")
