//@version=5
indicator("Pivot SR", overlay=true)
pivotLen = 3
ph = ta.pivothigh(high, pivotLen, pivotLen)
pl = ta.pivotlow(low, pivotLen, pivotLen)
var float lastHigh = 0.0
var float lastLow = 0.0
if not na(ph)
    lastHigh := ph
if not na(pl)
    lastLow := pl
plot(lastHigh)
plot(lastLow)
