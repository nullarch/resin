//@version=5
indicator("Keltner", overlay=true)
length = 10
mult = 1.5
basis = ta.ema(close, length)
atr = ta.atr(length)
upper = basis + mult * atr
lower = basis - mult * atr
squeeze = close > lower and close < upper
plot(basis)
plot(upper)
plot(lower)
plot(squeeze ? 1.0 : 0.0)
