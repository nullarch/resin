//@version=5
indicator("Enum Switch State")
enum State
    Idle
    Rising
    Falling
var int stateVal = 0
curState = close > open ? State.Rising : State.Falling
switch curState
    State.Rising =>
        stateVal := stateVal + 1
    State.Falling =>
        stateVal := stateVal - 1
