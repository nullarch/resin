
//@version=5
strategy("Vol Filter", overlay=true)
atr = ta.atr(3)
avgATR = ta.sma(atr, 5)
sma3 = ta.sma(close, 3)
lowVol = atr < avgATR * 1.2
if close > sma3 and lowVol
    strategy.entry("Long", strategy.long)
if close < sma3 or atr > avgATR * 2
    strategy.close("Long")
plot(sma3, "MA")
