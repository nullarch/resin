
//@version=5
indicator("Adapt LB")
atr = nz(ta.atr(3), 1.0)
lookback = atr > 2.0 ? 3 : 5
total = 0.0
count = 0
for i = 0 to lookback - 1
    if not na(close[i])
        total := total + close[i]
        count := count + 1
adaptMA = count > 0 ? total / count : close
plot(adaptMA, "AMA")
