
//@version=5
indicator("Pct Interp")
var float[] data = array.new_float(0)
array.push(data, close)
if array.size(data) > 8
    array.shift(data)
p25 = array.percentile_linear_interpolation(data, 25.0)
p75 = array.percentile_linear_interpolation(data, 75.0)
spread = p75 - p25
plot(spread, "IQR")
