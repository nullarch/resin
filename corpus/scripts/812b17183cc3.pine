
//@version=5
indicator("Vol Regime")
atr = ta.atr(3)
avgATR = ta.sma(atr, 5)
ratio = avgATR > 0 ? atr / avgATR : 1.0
regime = switch
    ratio > 2.0 => "extreme"
    ratio > 1.5 => "high"
    ratio > 0.8 => "normal"
    => "low"
isNormal = regime == "normal"
plot(isNormal ? 1 : 0, "Norm")
