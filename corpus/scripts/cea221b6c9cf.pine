
//@version=5
indicator("Reg Dur")
var int regimeDir = 0
var int duration = 0
sma3 = ta.sma(close, 3)
newDir = close > sma3 ? 1 : -1
if newDir == regimeDir
    duration := duration + 1
else
    regimeDir := newDir
    duration := 1
longRegime = duration > 3
plot(duration, "Dur")
