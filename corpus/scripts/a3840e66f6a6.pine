
//@version=5
indicator("ConsecBars")
bullish = close > open
var int streak = 0
var bool wasBull = false
same = bullish == wasBull
streak := same ? streak + 1 : 1
wasBull := bullish
plot(streak, "Str")
