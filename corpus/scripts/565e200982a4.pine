
//@version=5
indicator("Upper Shd")
topBody = math.max(close, open)
upperShadow = high - topBody
totalRange = high - low
shadowRatio = totalRange > 0 ? upperShadow / totalRange * 100 : 0.0
plot(shadowRatio, "US")
