
//@version=5
indicator("Sortino")
ret = close - close[1]
avgRet = ta.sma(ret, 5)
downRet = not na(ret) and ret < 0 ? ret : 0.0
downVar = ta.sma(math.pow(downRet, 2), 5)
downDev = math.sqrt(math.abs(downVar))
sortino = downDev > 0 ? nz(avgRet, 0.0) / downDev : 0.0
plot(sortino, "Sort")
