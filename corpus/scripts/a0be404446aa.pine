
//@version=5
strategy("Short Only", overlay=true)
sma3 = ta.sma(close, 3)
rsiVal = ta.rsi(close, 3)
if ta.crossunder(close, sma3) and rsiVal > 60
    strategy.entry("Short", strategy.short)
if rsiVal < 30
    strategy.close("Short")
plot(sma3, "SMA")
