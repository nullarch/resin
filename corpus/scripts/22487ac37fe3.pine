//@version=5
indicator("Switch No Expr ML")
rsi = ta.rsi(close, 5)
signal = switch
    rsi > 70 =>
        upper = ta.highest(high, 3)
        -1.0
    rsi < 30 =>
        lower = ta.lowest(low, 3)
        1.0
    =>
        0.0
plot(signal)
