//@version=5
indicator("test")
mode = "complex"
threshold = 100.0
result = switch mode
    "complex" =>
        base = close > threshold ? close * 1.1 : close * 0.9
        adjusted = base + 5
        adjusted
    "simple" => close
    => 0.0
plot(result)
