
//@version=5
indicator("Str Chain")
sma3 = ta.sma(close, 3)
rsi = ta.rsi(close, 3)
s = "P:" + str.tostring(close)
s := s + " S:" + str.tostring(sma3)
s := s + " R:" + str.tostring(rsi)
totalLen = str.length(s)
plot(totalLen, "Len")
