
//@version=5
indicator("Sig Filter")
rsi = nz(ta.rsi(close, 3), 50.0)
atr = ta.atr(3)
avgATR = ta.sma(atr, 5)
volState = avgATR > 0 ? atr / avgATR : 1.0
rawSig = rsi > 60 ? 1.0 : rsi < 40 ? -1.0 : 0.0
filtered = volState > 2.0 ? 0.0 : volState > 1.5 ? rawSig * 0.5 : rawSig
plot(filtered, "Filt")
