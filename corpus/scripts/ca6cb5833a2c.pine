
//@version=5
indicator("Insert Sort")
var float[] sorted = array.new_float(0)
if array.size(sorted) > 5
    array.shift(sorted)
idx = array.binary_search_leftmost(sorted, close)
array.insert(sorted, idx, close)
med = array.median(sorted)
plot(med, "Median")
