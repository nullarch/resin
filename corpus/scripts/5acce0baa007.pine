//@version=5
indicator("Switch Assign")
var float total = 0.0
direction = close > open ? 1 : close < open ? -1 : 0
switch direction
    1 =>
        total := total + high - low
    -1 =>
        total := total - (high - low)
    =>
        total := total
plot(total)
