
//@version=5
indicator("Run Avg")
data = array.from(close, open, high, low)
runSum = 0.0
runAvg = 0.0
count = 0
for val in data
    count := count + 1
    runSum := runSum + float(val)
    runAvg := runSum / count
plot(runAvg, "RunAvg")
