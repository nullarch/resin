
//@version=5
indicator("MA Cross")
ema3 = ta.ema(close, 3)
sma5 = ta.sma(close, 5)
bullCross = ta.crossover(ema3, sma5)
bearCross = ta.crossunder(ema3, sma5)
signal = bullCross ? 1 : bearCross ? -1 : 0
plot(signal, "Sig")
