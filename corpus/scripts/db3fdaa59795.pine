
//@version=5
indicator("Profit Targets")
atr = ta.atr(3)
target = close
step = atr * 0.5
levels = 0
while levels < 5
    target := target + step
    levels := levels + 1
finalTarget = target
distance = finalTarget - close
plot(distance, "Dist")
