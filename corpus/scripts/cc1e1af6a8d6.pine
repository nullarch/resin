
//@version=5
indicator("Trade Track")
var int trades = 0
var bool wasAbove = false
sma3 = ta.sma(close, 3)
isAbove = close > sma3
if isAbove and not wasAbove
    trades := trades + 1
wasAbove := isAbove
plot(trades, "Trades")
