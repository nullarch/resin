
//@version=5
indicator("Enum Switch")
enum Action
    Buy = 1
    Sell = -1
    Hold = 0

rsi = nz(ta.rsi(close, 3), 50.0)
act = rsi > 65 ? Action.Sell : rsi < 35 ? Action.Buy : Action.Hold
result = switch act
    Action.Buy => 100.0
    Action.Sell => -100.0
    => 0.0
plot(result, "Res")
