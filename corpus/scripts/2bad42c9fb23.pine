//@version=5
indicator("Entropy Estimate")
var float entropy = 0.0
rangeVal = high - low
total = high + low
p1 = total > 0 ? (close - low) / total : 0.5
p2 = 1.0 - p1
safeP1 = p1 > 0.01 ? p1 : 0.01
safeP2 = p2 > 0.01 ? p2 : 0.01
entropy := -(safeP1 * math.log(safeP1) + safeP2 * math.log(safeP2))
