//@version=5
indicator("ternary chain")
var float result = 0.0
sma3 = ta.sma(close, 3)
ema3 = ta.ema(close, 3)
signal = close > sma3 ? close > ema3 ? 2.0 : 1.0 : close < sma3 ? -2.0 : -1.0
result := signal
