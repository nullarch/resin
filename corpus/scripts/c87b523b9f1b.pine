
//@version=5
indicator("FC Errors")
var float[] errors = array.new_float(0)
sma3 = ta.sma(close, 3)
err = math.abs(close - sma3)
array.push(errors, err)
if array.size(errors) > 8
    array.shift(errors)
mae = array.avg(errors)
maxErr = array.max(errors)
plot(mae, "MAE")
