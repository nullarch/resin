//@version=5
indicator("drawing test")
if barstate.islast
    label.new(bar_index, close, "Last")
    line.new(bar_index - 1, close[1], bar_index, close)
    box.new(bar_index - 5, high, bar_index, low)
