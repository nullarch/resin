//@version=5
indicator("Map keys values")
var m = map.new<string, float>()
map.put(m, "open", open)
map.put(m, "high", high)
map.put(m, "low", low)
map.put(m, "close", close)
sz = map.size(m)
hasClose = map.contains(m, "close")
closeVal = map.get(m, "close")
