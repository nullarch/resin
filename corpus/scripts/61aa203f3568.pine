
//@version=5
indicator("Fmt Val")
formatScore(float val) =>
    val > 0.5 ? "HIGH" : val > 0 ? "MED" : val > -0.5 ? "LOW" : "VERY_LOW"

rsi = nz(ta.rsi(close, 3), 50.0)
score = (rsi - 50) / 50
label = formatScore(score)
isHigh = label == "HIGH"
plot(isHigh ? 1 : 0, "High")
