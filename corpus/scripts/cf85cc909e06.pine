
//@version=5
indicator("While Brk")
target = ta.sma(close, 5)
step = (close - target) / 10
current = close
iters = 0
while iters < 50
    current := current - step
    iters := iters + 1
    if math.abs(current - target) < 0.01
        break
plot(current, "Cur")
plot(iters, "Its")
