
//@version=5
indicator("Counter Reset")
var int counter = 0
var int resets = 0
counter := counter + 1
sma3 = ta.sma(close, 3)
if ta.crossover(close, sma3)
    resets := resets + 1
    counter := 0
plot(counter, "Counter")
plot(resets, "Resets")
