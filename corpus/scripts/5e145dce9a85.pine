
//@version=5
strategy("Vol Breakout", overlay=true)
atr = ta.atr(3)
sma5 = ta.sma(close, 5)
upperBreak = sma5 + atr * 1.5
lowerBreak = sma5 - atr * 1.5
if close > upperBreak
    strategy.entry("Long", strategy.long)
if close < lowerBreak
    strategy.close("Long")
plot(upperBreak, "Upper")
