//@version=5
indicator("FE07")
var float totalHist = 0.0
[macdLine, signalLine, hist] = ta.macd(close, 3, 5, 3)
absHist = math.abs(nz(hist, 0.0))
sum = 0.0
for i = 1 to 3
    sum := sum + absHist
if sum > 0 and close > open
    totalHist := totalHist + sum
