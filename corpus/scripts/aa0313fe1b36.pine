
//@version=5
indicator("Pos Size")
capital = 10000.0
riskPct = 2.0
atr = nz(ta.atr(3), 1.0)
stopDist = atr * 2.0
riskAmount = capital * riskPct / 100
posSize = stopDist > 0 ? riskAmount / stopDist : 0.0
plot(posSize, "PS")
