//@version=5
indicator("Newton Sqrt")
newtonSqrt(val) =>
    x = val > 0 ? val / 2.0 : 1.0
    i = 0
    while i < 10 and val > 0
        x := (x + val / x) / 2.0
        i := i + 1
    x
float sqrtClose = newtonSqrt(close)
plot(sqrtClose, "Sqrt")
