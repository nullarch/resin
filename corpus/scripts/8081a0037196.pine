//@version=5
indicator("NestedIfForTernArr")
arr = array.new_float(0)
if close > open
    for i = 0 to 3
        val = i > 1 ? 2.0 : 1.0
        array.push(arr, val)
else
    array.push(arr, 0.0)
plot(float(array.size(arr)), "Sz")
