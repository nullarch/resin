//@version=5
indicator("UDF If Return")
f_sign(x) =>
    if x > 0
        1.0
    else if x < 0
        -1.0
    else
        0.0

var float result = 0.0
result := f_sign(42.0) + f_sign(-5.0) + f_sign(0.0)
plot(result)
