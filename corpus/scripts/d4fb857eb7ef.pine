
//@version=5
indicator("Cooldown")
var int cooldown = 0
var int eventCount = 0

if cooldown > 0
    cooldown := cooldown - 1

if cooldown == 0 and close > open
    eventCount := eventCount + 1
    cooldown := 3
plot(eventCount, "EC")
