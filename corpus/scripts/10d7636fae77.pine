
//@version=5
indicator("BB Width")
[mid, upper, lower] = ta.bb(close, 5, 2.0)
bbWidth = mid > 0 ? (upper - lower) / mid * 100 : 0.0
avgWidth = ta.sma(bbWidth, 3)
squeeze = bbWidth < avgWidth * 0.75
plot(bbWidth, "Width")
plot(squeeze ? 1 : 0, "Sq")
