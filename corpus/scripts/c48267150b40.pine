
//@version=5
indicator("Trend Track")
var int trendDir = 0
var int trendLen = 0
sma3 = ta.sma(close, 3)
newDir = close > sma3 ? 1 : -1
if newDir == trendDir
    trendLen := trendLen + 1
else
    trendDir := newDir
    trendLen := 1
plot(trendDir, "Dir")
plot(trendLen, "Len")
