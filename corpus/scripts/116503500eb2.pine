
//@version=5
indicator("Stats Suite")
var float[] data = array.new_float(0)
array.push(data, close)
if array.size(data) > 5
    array.shift(data)
avg = array.avg(data)
med = array.median(data)
mx = array.max(data)
mn = array.min(data)
rng = array.range(data)
plot(rng, "Range")
