//@version=5
indicator("UDT method modify")
type Tracker
    float maxVal = 0.0
    int bars = 0
method update(Tracker t, float val) =>
    if val > t.maxVal
        t.maxVal := val
    t.bars := t.bars + 1
var tracker = Tracker.new()
tracker.update(close)
