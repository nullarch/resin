
//@version=5
indicator("Vol Thresh")
atr = nz(ta.atr(3), 1.0)
threshold = 10.0
steps = 0
while threshold > atr and steps < 20
    threshold := threshold * 0.7
    steps := steps + 1
plot(steps, "St")
