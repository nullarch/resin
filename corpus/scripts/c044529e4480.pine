
//@version=5
indicator("Inputs")
length = input.int(5, "Length")
mult = input.float(2.0, "Multiplier")
src = input.source(close, "Source")
showPlot = input.bool(true, "Show")
sma = ta.sma(src, length)
if showPlot
    plot(sma * mult, "Result")
