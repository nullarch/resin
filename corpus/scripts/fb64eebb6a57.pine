
//@version=5
indicator("Map Transform")
src = map.new<string, float>()
map.put(src, "a", 10.0)
map.put(src, "b", 20.0)
map.put(src, "c", 30.0)
var dst = map.new<string, float>()
for [k, v] in src
    map.put(dst, k, v * 2)
total = 0.0
for [k, v] in dst
    total := total + v
plot(total, "Total")
