//@version=5
indicator("UDT Defaults")
var float result = 0.0

type Config
    int period = 14
    float multiplier = 2.0
    string label = "default"

cfg = Config.new()
result := cfg.period + cfg.multiplier
