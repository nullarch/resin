
//@version=5
indicator("Level Wt")
hi5 = ta.highest(high, 5)
lo5 = ta.lowest(low, 5)
rng = hi5 - lo5
pos = rng > 0 ? (close - lo5) / rng * 100 : 50.0
weight = pos > 80 ? 0.5 : pos < 20 ? 0.5 : 1.0
rsi = nz(ta.rsi(close, 3), 50.0)
signal = (rsi - 50) / 50 * weight
plot(signal, "Sig")
