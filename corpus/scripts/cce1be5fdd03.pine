
//@version=5
indicator("Vol Hist")
var float[] volHist = array.new_float(0)
atr = nz(ta.atr(3), 1.0)
array.push(volHist, atr)
if array.size(volHist) > 8
    array.shift(volHist)
avgVol = array.size(volHist) > 0 ? array.avg(volHist) : atr
rising = atr > avgVol
plot(rising ? 1 : 0, "R")
