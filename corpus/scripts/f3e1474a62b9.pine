
//@version=5
indicator("Level Hist")
var float[] pivots = array.new_float(0)
pivot = (high + low + close) / 3
array.push(pivots, pivot)
if array.size(pivots) > 8
    array.shift(pivots)
avgPivot = array.size(pivots) > 0 ? array.avg(pivots) : pivot
plot(avgPivot, "AP")
