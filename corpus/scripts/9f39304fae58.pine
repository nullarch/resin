
//@version=5
indicator("Multi Alert")
sma3 = ta.sma(close, 3)
rsi = nz(ta.rsi(close, 3), 50.0)
avgVol = ta.sma(volume, 5)
cond1 = close > sma3
cond2 = rsi > 55
cond3 = volume > avgVol
allTrue = cond1 and cond2 and cond3
anyTrue = cond1 or cond2 or cond3
plot(allTrue ? 2 : anyTrue ? 1 : 0, "A")
