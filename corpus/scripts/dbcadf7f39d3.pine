
//@version=5
indicator("Mean Reg")
mean5 = ta.sma(close, 5)
deviation = close - mean5
nextBar = close - close[1]
sameDir = not na(nextBar) and ((deviation > 0 and nextBar < 0) or (deviation < 0 and nextBar > 0))
mrStrength = sameDir ? 1.0 : 0.0
avgMR = ta.sma(mrStrength, 5)
plot(avgMR, "MR")
