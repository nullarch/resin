
//@version=5
indicator("Mom Metrics")
var mm = map.new<string, float>()
rsi = nz(ta.rsi(close, 3), 50.0)
mom = close - close[1]
map.put(mm, "rsi", rsi)
map.put(mm, "momentum", nz(mom, 0.0))
map.put(mm, "speed", math.abs(nz(mom, 0.0)))
speed = map.get(mm, "speed", 0.0)
plot(speed, "Spd")
