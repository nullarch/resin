//@version=5
indicator("HaarRecon")
prev = nz(close[1], close)
approx = (close + prev) / 2.0
detail = (close - prev) / 2.0
recon = approx + detail
diff = math.abs(recon - close)
plot(diff, "ReconErr")
