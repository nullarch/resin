
//@version=5
indicator("Standardize")
var float[] data = array.new_float(0)
array.push(data, close)
if array.size(data) > 6
    array.shift(data)
std = array.standardize(data)
lastStd = array.size(std) > 0 ? array.last(std) : 0.0
plot(lastStd, "StdZ")
