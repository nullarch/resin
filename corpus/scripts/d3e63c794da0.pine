//@version=5
indicator("Pivot HL")
pivHi = ta.pivothigh(high, 2, 2)
pivLo = ta.pivotlow(low, 2, 2)
var float lastPivHi = 0.0
var float lastPivLo = 999999.0
if not na(pivHi)
    lastPivHi := pivHi
if not na(pivLo)
    lastPivLo := pivLo
