//@version=5
indicator("Var Types")
var float maxClose = 0.0
var int barCount = 0
var bool triggered = false
if close > maxClose
    maxClose := close
barCount += 1
if barCount > 5 and not triggered
    triggered := true
plot(maxClose)
