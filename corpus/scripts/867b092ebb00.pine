//@version=5
indicator("UDF For Return")
f_sum_to(n) =>
    float total = 0.0
    for i = 1 to n
        total := total + float(i)
    total

var float result = 0.0
result := f_sum_to(10)
plot(result)
