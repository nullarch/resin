
//@version=5
indicator("FC Conv")
target = ta.sma(close, 5)
estimate = close
alpha = 0.3
iters = 0
while math.abs(estimate - target) > 0.01 and iters < 50
    estimate := estimate + (target - estimate) * alpha
    iters := iters + 1
plot(iters, "Its")
