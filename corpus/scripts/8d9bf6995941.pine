
//@version=5
indicator("For UDF")
barScore(int idx) =>
    c = close[idx]
    o = open[idx]
    not na(c) and not na(o) ? (c > o ? 1.0 : -1.0) : 0.0

total = 0.0
for i = 0 to 4
    total := total + barScore(i)
avgScore = total / 5
plot(avgScore, "Avg")
