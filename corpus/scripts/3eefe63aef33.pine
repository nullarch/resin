
//@version=5
indicator("Multi Cond For")
found = false
target = 0
for i = 1 to 8
    c = close[i]
    if na(c)
        break
    if c > close and c > 106
        target := i
        found := true
        break
plot(found ? target : -1, "Target")
