
//@version=5
indicator("RegStrength")
rsi = nz(ta.rsi(close, 3), 50.0)
atr = nz(ta.atr(3), 1.0)
bullBar = close > open ? 1.0 : 0.0
rsiScore = rsi > 60 ? 1.0 : rsi < 40 ? -1.0 : 0.0
volScore = atr > 1.5 ? 1.0 : 0.0
composite = rsiScore + volScore + bullBar
plot(composite, "CS")
