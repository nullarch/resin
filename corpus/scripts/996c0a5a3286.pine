
//@version=5
indicator("RSI Div")
rsi = nz(ta.rsi(close, 3), 50.0)
priceHigher = not na(close[3]) and close > close[3]
rsiLower = not na(close[3]) and rsi < 55
bearDiv = priceHigher and rsiLower
plot(bearDiv ? 1 : 0, "BD")
