//@version=5
indicator("Var ML")
var float peak = 0.0
var float trough = 99999.0
if close > peak and
    close > open
    peak := close
if close < trough and
    close < open
    trough := close
plot(peak - trough)
