//@version=5
strategy("test", pyramiding=3)
rsi = ta.rsi(close, 5)
ema = ta.ema(close, 3)
if rsi < 30
    strategy.entry("L1", strategy.long)
if rsi < 20 and close > ema
    strategy.entry("L2", strategy.long)
if rsi > 70
    strategy.close_all()
