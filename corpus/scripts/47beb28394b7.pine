//@version=5
indicator("Drawing Noop")
l = line.new(bar_index, close, bar_index[1], close[1])
line.delete(l)
lb = label.new(bar_index, close, "test")
label.delete(lb)
b = box.new(bar_index, high, bar_index[1], low)
box.delete(b)
plot(close)
