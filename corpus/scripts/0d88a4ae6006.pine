
//@version=5
indicator("BatchNorm")
src = close
mean = nz(ta.sma(src, 5), src)
dev = nz(ta.stdev(src, 5), 1.0)
normalized = dev > 0.001 ? (src - mean) / dev : 0.0
gamma = 1.0
beta = 0.0
out = gamma * normalized + beta
plot(out, "BN")
