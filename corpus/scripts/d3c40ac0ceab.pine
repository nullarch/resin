//@version=5
indicator("Multi UDF TA")
calcRSI(src, len) =>
    ta.rsi(src, len)

calcSMA(src, len) =>
    ta.sma(src, len)

rsi_ = calcRSI(close, 5)
sma_ = calcSMA(close, 3)
combined = nz(rsi_) + nz(sma_)
plot(combined)
