//@version=5
indicator("Enum Ternary")
UP = 1
DOWN = -1
FLAT = 0

var int dir = 0
dir := close > close[1] ? UP : close < close[1] ? DOWN : FLAT

var string label_ = ""
label_ := dir == UP ? "rising" : dir == DOWN ? "falling" : "flat"
plot(close)
