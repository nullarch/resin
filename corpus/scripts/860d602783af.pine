//@version=5
indicator("Quad Variation")
float prevC = not na(close[1]) ? close[1] : close
float sqRet = math.pow(close - prevC, 2)
var float quadVar = 0.0
quadVar := quadVar + sqRet
plot(quadVar, "QV")
