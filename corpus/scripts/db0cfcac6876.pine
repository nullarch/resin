
//@version=5
indicator("Regime Lbl")
sma3 = ta.sma(close, 3)
atr = ta.atr(3)
avgATR = ta.sma(atr, 5)
trend = close > sma3 ? "Up" : "Down"
vol = avgATR > 0 and atr > avgATR * 1.3 ? "HiVol" : "LoVol"
label = trend + "_" + vol
isFavorable = label == "Up_LoVol"
plot(isFavorable ? 1 : 0, "Fav")
