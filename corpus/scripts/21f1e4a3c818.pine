
//@version=5
indicator("Decay")
var float signal = 0.0
rsi = nz(ta.rsi(close, 3), 50.0)
if rsi > 70 or rsi < 30
    signal := rsi > 70 ? 1.0 : -1.0
else
    signal := signal * 0.9
plot(signal, "Sig")
