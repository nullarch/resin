
//@version=5
indicator("UDT Hist")
type History
    float current = 0.0
    float previous = 0.0
    float change = 0.0

var History h = History.new()
h.previous := h.current
h.current := close
h.change := h.current - h.previous
plot(h.change, "Chg")
