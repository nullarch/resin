//@version=5
indicator("Map Full")
var m = map.new<string, float>()
map.put(m, "close", close)
map.put(m, "open", open)
has = map.contains(m, "close")
val = map.get(m, "close")
sz = map.size(m)
keys = map.keys(m)
