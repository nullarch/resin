
//@version=5
indicator("Reassign If")
val = 0.0
rsi = nz(ta.rsi(close, 3), 50.0)
if rsi > 60
    val := 1.0
    val := val + 0.5
else if rsi > 40
    val := 0.0
else
    val := -1.0
    val := val - 0.5
plot(val, "Val")
