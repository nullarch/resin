//@version=5
indicator("Var Cond Accum")
var float up_sum = 0.0
var float dn_sum = 0.0
if close > open
    up_sum := up_sum + (close - open)
else
    dn_sum := dn_sum + (open - close)
var float result = 0.0
result := up_sum - dn_sum
plot(result)
