
//@version=5
indicator("State Conv")
rsi = nz(ta.rsi(close, 3), 50.0)
target = rsi > 50 ? 1.0 : -1.0
estimate = 0.0
iters = 0
while math.abs(estimate - target) > 0.1 and iters < 20
    estimate := estimate + (target - estimate) * 0.3
    iters := iters + 1
plot(iters, "It")
