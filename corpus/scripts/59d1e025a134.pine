
//@version=5
indicator("UDFComp")
getResidual(src, trendVal) =>
    src - trendVal

trend = nz(ta.ema(close, 3), close)
res = getResidual(close, trend)
magnitude = math.abs(res)
plot(magnitude, "Mag")
