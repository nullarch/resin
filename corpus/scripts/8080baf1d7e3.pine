
//@version=5
indicator("Risk Level")
atr = ta.atr(3)
rsi = ta.rsi(close, 3)
atrHigh = atr > ta.sma(atr, 5)
rsiExtreme = rsi > 75 or rsi < 25
riskLevel = switch
    atrHigh and rsiExtreme => 4
    atrHigh => 3
    rsiExtreme => 2
    => 1
plot(riskLevel, "Risk")
