
//@version=5
indicator("Pivots")
ph = ta.pivothigh(high, 2, 2)
pl = ta.pivotlow(low, 2, 2)
hasPivotHigh = not na(ph)
hasPivotLow = not na(pl)
plot(hasPivotHigh ? 1 : 0, "PH")
plot(hasPivotLow ? 1 : 0, "PL")
