
//@version=5
indicator("KER")
erLen = 5
direction = math.abs(close - nz(close[4], close))
volatility = 0.0
for i = 0 to 3
    prev = nz(close[i + 1], close)
    volatility := volatility + math.abs(close[i] - prev)
er = volatility > 0 ? direction / volatility : 0.0
plot(er, "ER")
