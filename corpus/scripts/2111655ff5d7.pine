
//@version=5
indicator("Multi Speed")
ultraFast = ta.ema(close, 2)
fast = ta.ema(close, 3)
medium = ta.sma(close, 5)
alignUp = ultraFast > fast and fast > medium
alignDown = ultraFast < fast and fast < medium
momentum = alignUp ? 1 : alignDown ? -1 : 0
plot(momentum, "Mom")
