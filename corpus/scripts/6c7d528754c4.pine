//@version=5
indicator("ForArrSwitchUdf")
classify(val) =>
    switch
        val > 5.0 => 3.0
        val > 2.0 => 2.0
        => 1.0
arr = array.new_float(0)
for i = 0 to 4
    array.push(arr, classify(float(i)))
plot(array.sum(arr), "S")
