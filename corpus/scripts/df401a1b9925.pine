
//@version=5
strategy("Vol Break", overlay=true)
sma3 = ta.sma(close, 3)
avgVol = ta.sma(volume, 5)
volBreak = volume > avgVol * 1.5
priceBreak = close > sma3
if volBreak and priceBreak
    strategy.entry("Long", strategy.long)
if close < sma3
    strategy.close("Long")
plot(sma3, "MA")
