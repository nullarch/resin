//@version=5
indicator("NA Dual")
float x = na  // non-var: na each bar before assignment
var bool check1 = false
check1 := na(x)  // should be True (x is na)
x := 42.0
var bool check2 = true
check2 := na(x)  // should be False (x is 42.0 now)
// nz with na fallback
var float safe = 0.0
safe := nz(na, 99.0)  // na → 99.0
plot(close)
