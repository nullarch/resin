//@version=5
indicator("ZeroSumVal")
spread = high - low
retAbs = math.abs(close - open)
a11 = retAbs
a12 = -spread
a21 = spread
a22 = -retAbs
denom = a11 - a12 - a21 + a22
gameVal = math.abs(denom) > 0.01 ? (a11 * a22 - a12 * a21) / denom : 0.0
plot(gameVal, "GVal")
