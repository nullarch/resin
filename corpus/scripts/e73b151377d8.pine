
//@version=5
indicator("UDF Tuple")
calcBands(float src, int len, float mult) =>
    basis = ta.sma(src, len)
    dev = ta.stdev(src, len) * mult
    [basis, basis + dev, basis - dev]

[mid, upper, lower] = calcBands(close, 5, 1.5)
width = upper - lower
plot(width, "Width")
