//@version=5
indicator("COG Loop")
pine_cog(float source, int length) =>
    s = math.sum(source, length)
    num = 0.0
    for i = 0 to length - 1
        price = source[i]
        num := num + price * (i + 1)
    s != 0 ? -num / s : 0.0
result = pine_cog(close, 5)
plot(result)
