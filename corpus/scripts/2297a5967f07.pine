
//@version=5
indicator("Osc Map")
var om = map.new<string, float>()
rsi = nz(ta.rsi(close, 3), 50.0)
k = nz(ta.stoch(close, high, low, 3), 50.0)
map.put(om, "rsi", rsi)
map.put(om, "stoch", k)
obCount = 0
for [key, val] in om
    if val > 70
        obCount := obCount + 1
plot(obCount, "OB")
