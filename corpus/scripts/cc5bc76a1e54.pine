
//@version=5
indicator("FC Label")
sma3 = ta.sma(close, 3)
slope = sma3 - sma3[1]
dir = nz(slope, 0.0) > 0 ? "UP" : "DN"
conf = math.abs(nz(slope, 0.0)) > 1 ? "HIGH" : "LOW"
label = dir + "_" + conf
isHighConf = str.contains(label, "HIGH")
plot(isHighConf ? 1 : 0, "HiC")
