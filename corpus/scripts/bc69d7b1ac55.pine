
//@version=5
indicator("Str Scan")
sma5 = ta.sma(close, 5)
aboveCount = 0
for i = 0 to 4
    if not na(close[i]) and close[i] > sma5
        aboveCount := aboveCount + 1
strengthPct = aboveCount / 5.0 * 100
plot(strengthPct, "SP")
