//@version=5
indicator("While Search")
arr = array.from(5.0, 10.0, 15.0, 20.0, 25.0)
target = 15.0
var int foundIdx = -1
idx = 0
while idx < arr.size()
    if arr.get(idx) == target
        foundIdx := idx
        break
    idx := idx + 1
plot(foundIdx)
