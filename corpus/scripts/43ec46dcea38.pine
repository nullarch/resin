
//@version=5
indicator("Str Wt")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
rs = sma5 > 0 ? sma3 / sma5 : 1.0
weight = rs > 1.02 ? 1.5 : rs > 1.0 ? 1.0 : rs > 0.98 ? 0.5 : 0.25
rsi = nz(ta.rsi(close, 3), 50.0)
signal = (rsi - 50) / 50 * weight
plot(signal, "Sig")
