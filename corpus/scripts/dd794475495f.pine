
//@version=5
indicator("Lin WA")
sumWt = 0.0
sumVal = 0.0
for i = 0 to 4
    wt = float(5 - i)
    val = close[i]
    if not na(val)
        sumWt := sumWt + wt
        sumVal := sumVal + val * wt
lwma = sumWt > 0 ? sumVal / sumWt : close
plot(lwma, "LWMA")
