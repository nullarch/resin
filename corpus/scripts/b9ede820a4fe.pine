
//@version=5
indicator("Trend Regime")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
spread = sma5 > 0 ? (sma3 - sma5) / sma5 * 100 : 0.0
regime = spread > 1.0 ? 2 : spread > 0 ? 1 : spread > -1.0 ? -1 : -2
plot(regime, "TR")
