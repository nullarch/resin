
//@version=5
indicator("Risk Scan")
maxRisk = 0.0
for i = 0 to 4
    c = close[i]
    h = high[i]
    l = low[i]
    if not na(c) and not na(h) and not na(l)
        barRisk = c > 0 ? (h - l) / c * 100 : 0.0
        maxRisk := math.max(maxRisk, barRisk)
plot(maxRisk, "MR")
