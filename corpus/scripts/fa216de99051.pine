
//@version=5
indicator("Standardize")
var float[] data = array.new_float(0)
array.push(data, close)
if array.size(data) > 5
    array.shift(data)
standardized = array.standardize(data)
lastZ = array.size(standardized) > 0 ? array.last(standardized) : 0.0
plot(lastZ, "ZScore")
