
//@version=5
indicator("MA Conv")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
spread = math.abs(sma3 - sma5)
threshold = 5.0
steps = 0
while threshold > 0.1 and threshold > spread
    threshold := threshold * 0.5
    steps := steps + 1
plot(steps, "St")
