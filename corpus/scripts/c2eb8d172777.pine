
//@version=5
indicator("Roll MDD")
var float[] equity = array.new_float(0)
array.push(equity, close)
if array.size(equity) > 8
    array.shift(equity)
peak = array.max(equity)
current = array.last(equity)
dd = peak > 0 ? (peak - current) / peak * 100 : 0.0
plot(dd, "DD%")
