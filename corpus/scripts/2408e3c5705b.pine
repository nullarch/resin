
//@version=5
indicator("VP Scan")
highVolBars = 0
avgVol = ta.sma(volume, 5)
for i = 0 to 4
    if not na(volume[i]) and volume[i] > avgVol
        highVolBars := highVolBars + 1
plot(highVolBars, "HVB")
