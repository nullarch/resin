
//@version=5
indicator("Switch Assign")
rsiVal = ta.rsi(close, 3)
mult = switch
    rsiVal > 70 => 0.5
    rsiVal > 50 => 1.0
    rsiVal > 30 => 1.5
    => 2.0
adjusted = close * mult
plot(adjusted, "Adj")
