//@version=5
indicator("Neighborhood Density")
var cells = array.new_int(5, 0)
state = close > open ? 1 : 0
array.unshift(cells, state)
if array.size(cells) > 5
    array.pop(cells)
density = 0
for i = 0 to math.min(array.size(cells) - 1, 4)
    density := density + array.get(cells, i)
