
//@version=5
indicator("MACD Hist")
[m, s, h] = ta.macd(close, 3, 5, 3)
var int histTrend = 0
if h > h[1]
    histTrend := histTrend + 1
else if h < h[1]
    histTrend := histTrend - 1
else
    histTrend := 0
plot(histTrend, "HT")
