
//@version=5
indicator("Vol Score")
volumeScore(float vol, float avgVol, bool priceUp) =>
    ratio = avgVol > 0 ? vol / avgVol : 1.0
    direction = priceUp ? 1.0 : -1.0
    ratio * direction

avgVol = ta.sma(volume, 5)
vScore = volumeScore(volume, avgVol, close > open)
plot(vScore, "VS")
