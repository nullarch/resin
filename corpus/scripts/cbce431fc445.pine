
//@version=5
indicator("Adapt MA")
var float adaptMA = 0.0
atr = ta.atr(3)
avgATR = ta.sma(atr, 5)
volRatio = avgATR > 0 ? math.min(atr / avgATR, 2.0) : 1.0
alpha = 2.0 / (3 + 1) * volRatio
adaptMA := alpha * close + (1 - alpha) * adaptMA
plot(adaptMA, "AMA")
