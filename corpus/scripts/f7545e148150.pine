
//@version=5
indicator("UDT Array Field")
type Tracker
    float total = 0.0
    int count = 0

var Tracker t = Tracker.new()
var float[] vals = array.new_float(0)
array.push(vals, close)
t.total := t.total + close
t.count := t.count + 1
avg = t.total / t.count
plot(avg, "Avg")
