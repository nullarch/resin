
//@version=5
strategy("Chan MR", overlay=true)
hi5 = ta.highest(high, 5)
lo5 = ta.lowest(low, 5)
mid = (hi5 + lo5) / 2.0
if close < lo5 + (hi5 - lo5) * 0.2
    strategy.entry("Long", strategy.long)
if close > mid
    strategy.close("Long")
plot(mid, "Mid")
