//@version=5
indicator("map crud")
var float result = 0.0
m = map.new<string, float>()
map.put(m, "a", 1.0)
map.put(m, "b", 2.0)
map.put(m, "c", 3.0)
has = map.contains(m, "b")
map.remove(m, "c")
result := has ? map.get(m, "a") + map.get(m, "b") : 0.0
