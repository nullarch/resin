
//@version=5
indicator("UDT Defaults")
type Config
    int period = 14
    float multiplier = 2.0
    string mode = "sma"
    bool enabled = true

cfg = Config.new()
smaVal = ta.sma(close, cfg.period > 10 ? 5 : 3)
plot(smaVal, "SMA")
