//@version=5
indicator("Bigram Transitions")
var int upUp = 0
var int upDown = 0
currUp = close > open ? 1 : 0
prevUp = close[1] > open[1] ? 1 : 0
if not na(close[1])
    if prevUp == 1 and currUp == 1
        upUp := upUp + 1
    if prevUp == 1 and currUp == 0
        upDown := upDown + 1
