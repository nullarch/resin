
//@version=5
indicator("Vol Regime")
avgVol = ta.sma(volume, 5)
ratio = avgVol > 0 ? volume / avgVol : 1.0
regime = switch
    ratio > 3.0 => "climax"
    ratio > 1.5 => "high"
    ratio > 0.7 => "normal"
    => "dry"
isClimax = regime == "climax"
plot(isClimax ? 1 : 0, "Cli")
