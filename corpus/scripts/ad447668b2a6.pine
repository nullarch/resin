
//@version=5
indicator("Up Bars")
consecutive = 0
for i = 0 to 4
    c = close[i]
    o = open[i]
    if na(c) or na(o)
        break
    if c > o
        consecutive := consecutive + 1
    else
        break
plot(consecutive, "ConsecutiveUp")
