//@version=5
indicator("TupleReturn")
var float avgResult = 0
var float devResult = 0
calcStats(src, len) =>
    avg = ta.sma(src, len)
    dev = ta.stdev(src, len)
    [avg, dev]
[myAvg, myDev] = calcStats(close, 3)
if not na(myAvg) and not na(myDev)
    avgResult := myAvg
    devResult := myDev
