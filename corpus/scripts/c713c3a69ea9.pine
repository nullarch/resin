
//@version=5
indicator("Dual Spread")
fast = ta.ema(close, 3)
slow = ta.sma(close, 5)
spread = fast - slow
spreadMA = ta.sma(spread, 3)
zscore = ta.stdev(spread, 5) > 0 ? (spread - spreadMA) / ta.stdev(spread, 5) : 0.0
plot(zscore, "Z")
