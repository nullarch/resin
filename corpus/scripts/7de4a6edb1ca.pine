
//@version=5
indicator("TernStr")
rsi = nz(ta.rsi(close, 3), 50.0)
strength = rsi > 80 ? "EXTREME" : rsi > 65 ? "STRONG" : rsi > 50 ? "MILD" : rsi > 35 ? "WEAK" : "EXTREME_BEAR"
prefix = close > open ? "BULL_" : "BEAR_"
label_ = prefix + strength
labelLen = str.length(label_)
plot(labelLen, "LL")
