
//@version=5
indicator("Covariance")
var float[] prices = array.new_float(0)
var float[] volumes = array.new_float(0)
array.push(prices, close)
array.push(volumes, volume)
cov = array.size(prices) >= 2 ? array.covariance(prices, volumes) : 0.0
plot(cov, "Cov")
