
//@version=5
indicator("Multi RSI")
var float[] rsiVals = array.new_float(0)
array.clear(rsiVals)
for p = 3 to 5
    r = ta.rsi(close, p)
    array.push(rsiVals, nz(r, 50.0))
avgRSI = array.avg(rsiVals)
plot(avgRSI, "AvgRSI")
