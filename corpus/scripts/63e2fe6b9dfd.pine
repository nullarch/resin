
//@version=5
indicator("Vol Hist")
var float[] volHist = array.new_float(0)
array.push(volHist, volume)
if array.size(volHist) > 8
    array.shift(volHist)
avgVol = array.avg(volHist)
maxVol = array.max(volHist)
relVol = maxVol > 0 ? volume / maxVol : 0.0
plot(relVol, "Rel")
