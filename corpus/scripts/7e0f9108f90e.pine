
//@version=5
indicator("MA Lbl")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
trend = sma3 > sma5 ? "BULL" : "BEAR"
position = close > sma3 ? "ABOVE" : "BELOW"
label = trend + "_" + position
isBullAbove = label == "BULL_ABOVE"
plot(isBullAbove ? 1 : 0, "BA")
