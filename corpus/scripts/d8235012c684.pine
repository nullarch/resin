
//@version=5
indicator("Osc Data")
var od = map.new<string, float>()
rsi = nz(ta.rsi(close, 3), 50.0)
k = nz(ta.stoch(close, high, low, 3), 50.0)
map.put(od, "rsi", rsi)
map.put(od, "stoch", k)
map.put(od, "avg", (rsi + k) / 2)
avgOsc = map.get(od, "avg", 50.0)
plot(avgOsc, "AO")
