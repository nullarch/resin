//@version=5
indicator("Map Contains")
var float result = 0.0
m = map.new<string, float>()
m.put("a", 1.0)
m.put("b", 2.0)
bool has_a = m.contains("a")
m.remove("a")
bool has_a_after = m.contains("a")
result := (has_a ? 10.0 : 0.0) + (has_a_after ? 1.0 : 0.0)
