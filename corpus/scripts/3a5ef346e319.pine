
//@version=5
indicator("Risk Lv")
atr = nz(ta.atr(3), 1.0)
riskPct = atr / close * 100
allocation = switch
    riskPct > 3.0 => 0.25
    riskPct > 2.0 => 0.5
    riskPct > 1.0 => 0.75
    => 1.0
plot(allocation, "Alloc")
