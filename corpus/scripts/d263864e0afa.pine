
//@version=5
indicator("OBV Proxy")
var float obv = 0.0
if close > close[1]
    obv := obv + volume
else if close < close[1]
    obv := obv - volume
obvMA = ta.sma(obv, 5)
plot(obv, "OBV")
plot(obvMA, "MA")
