//@version=5
indicator("Wasserstein Proxy")
float hh = ta.highest(high, 3)
float ll = ta.lowest(low, 3)
float rangeNow = hh - ll
float prevRange = not na(close[1]) ? (ta.highest(high, 3) - ta.lowest(low, 3)) : rangeNow
float wassDist = math.abs(rangeNow - prevRange)
plot(wassDist, "WassDist")
