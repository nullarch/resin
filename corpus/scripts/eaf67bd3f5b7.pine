
//@version=5
indicator("Drawdown")
var float peak = 0.0
var float maxDD = 0.0
if close > peak
    peak := close
dd = (peak - close) / peak * 100
if dd > maxDD
    maxDD := dd
plot(dd, "DD%")
plot(maxDD, "MaxDD%")
