
//@version=5
indicator("Pos Size UDF")
positionSize(float capital, float riskPct, float stopDist) =>
    riskAmt = capital * riskPct / 100
    stopDist > 0 ? riskAmt / stopDist : 0.0

atr = nz(ta.atr(3), 1.0)
stopDist = atr * 2
size = positionSize(10000.0, 2.0, stopDist)
plot(size, "PS")
