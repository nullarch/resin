
//@version=5
indicator("UDF Str")
classify(float val, float threshold) =>
    val > threshold ? "above" : val < -threshold ? "below" : "neutral"

change = close - ta.sma(close, 3)
atr = ta.atr(3)
cls = classify(change, atr)
isAbove = cls == "above"
plot(isAbove ? 1 : 0, "Above")
