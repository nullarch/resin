//@version=5
indicator("MLFQ")
var int queueLevel = 0
float burstTime = high - low
float threshold = nz(ta.sma(high - low, 3), 5.0)
if burstTime > threshold
    queueLevel := math.min(queueLevel + 1, 3)
else
    queueLevel := math.max(queueLevel - 1, 0)
float queuePriority = 4.0 - queueLevel
