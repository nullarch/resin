//@version=5
indicator("Math Edge")
float sq = math.sqrt(-1.0)
float lg = math.log(0.0)
var bool sq_na = na(sq)
var bool lg_na = na(lg)
// math.exp large but finite
float ex = math.exp(1.0)
var float result = 0.0
result := (sq_na ? 1.0 : 0.0) + (lg_na ? 10.0 : 0.0) + ex
plot(result)
