
//@version=5
indicator("Rev Target")
sma5 = ta.sma(close, 5)
price = close
steps = 0
while math.abs(price - sma5) > 0.1 and steps < 20
    price := price + (sma5 - price) * 0.3
    steps := steps + 1
plot(steps, "St")
