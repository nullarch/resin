
//@version=5
strategy("MR BB", overlay=true)
[mid, upper, lower] = ta.bb(close, 5, 2.0)
rsi = nz(ta.rsi(close, 3), 50.0)
if close < lower and rsi < 35
    strategy.entry("Long", strategy.long)
if close > mid or rsi > 65
    strategy.close("Long")
plot(mid, "Mid")
