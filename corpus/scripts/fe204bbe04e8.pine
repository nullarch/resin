
//@version=5
indicator("Vol Map FI")
var vm = map.new<string, float>()
atr = nz(ta.atr(3), 1.0)
map.put(vm, "atr", atr)
map.put(vm, "range", high - low)
map.put(vm, "body", math.abs(close - open))
total = 0.0
for [k, v] in vm
    total := total + v
plot(total, "T")
