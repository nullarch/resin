//@version=5
indicator("Adaptive Quant")
float atrVal = nz(ta.atr(3), 1.0)
float adaptStep = math.max(atrVal * 0.5, 0.5)
float quantized = math.round(close / adaptStep) * adaptStep
float quantErr = close - quantized
