//@version=5
indicator("FIFO Page Replace")
pages = array.new<float>(0)
int capacity = 4
float newPage = math.floor(close) % 10
pages.push(newPage)
if pages.size() > capacity
    pages.shift()
float resident = pages.size()
float hitRatio = resident / capacity
