
//@version=5
indicator("Window MinMax")
var float[] win = array.new_float(0)
array.push(win, close)
if array.size(win) > 5
    array.shift(win)
mx = array.max(win)
mn = array.min(win)
mid = (mx + mn) / 2
pos = (close - mn) / (mx - mn)
plot(pos, "Position")
