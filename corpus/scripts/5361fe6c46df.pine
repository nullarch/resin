//@version=5
indicator("ExpPayoff")
a11 = close - open
a12 = high - close
a21 = close - low
a22 = high - low
p = 0.6
q = 0.4
ep = p * q * a11 + p * (1.0 - q) * a12 + (1.0 - p) * q * a21 + (1.0 - p) * (1.0 - q) * a22
plot(ep, "EP")
