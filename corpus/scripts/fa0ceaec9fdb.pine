
//@version=5
indicator("Stoch Smooth")
k = ta.stoch(close, high, low, 5)
smoothK = ta.sma(k, 3)
smoothD = ta.sma(smoothK, 3)
bullCross = ta.crossover(smoothK, smoothD)
plot(smoothK, "K")
plot(smoothD, "D")
plot(bullCross ? 1 : 0, "Cross")
