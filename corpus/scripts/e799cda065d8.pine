
//@version=5
indicator("UDTDecomp")
type DecompResult
    float trend = 0.0
    float residual = 0.0
    int barNum = 0

trendVal = nz(ta.sma(close, 3), close)
resVal = close - trendVal
dr = DecompResult.new(trendVal, resVal, bar_index)
plot(dr.trend, "T")
