
//@version=5
indicator("Geo Mean")
logSum = 0.0
count = 0
for i = 0 to 4
    val = close[i]
    if not na(val) and val > 0
        logSum := logSum + math.log(val)
        count := count + 1
geoMean = count > 0 ? math.exp(logSum / count) : close
plot(geoMean, "GeoMean")
