
//@version=5
indicator("MTF Confirm")
fastMA = ta.sma(close, 3)
slowMA = ta.sma(close, 5)
rsi = ta.rsi(close, 3)
confirmed = false
if close > fastMA
    if fastMA > slowMA
        if rsi > 45
            confirmed := true
plot(confirmed ? 1 : 0, "Confirmed")
