
//@version=5
indicator("TF Params")
vol = ta.atr(3)
sma5 = ta.sma(vol, 5)
regime = vol > sma5 * 1.5 ? "high" : vol > sma5 ? "medium" : "low"
lookback = switch regime
    "high" => 3
    "medium" => 5
    "low" => 8
    => 5
smaAdaptive = ta.sma(close, lookback)
plot(smaAdaptive, "Adaptive")
