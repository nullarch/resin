
//@version=5
indicator("Adapt Period")
atr = ta.atr(3)
avgATR = ta.sma(atr, 5)
volRatio = avgATR > 0 ? atr / avgATR : 1.0
period = volRatio > 1.5 ? 3 : volRatio > 1.0 ? 4 : 5
adaptMA = ta.sma(close, period)
plot(adaptMA, "AMA")
