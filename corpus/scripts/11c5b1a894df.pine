//@version=5
indicator("Steady State")
var float level = 0.0
var int doses = 0
dose = close > open ? 10.0 : 0.0
elimination = level * 0.2
level := level + dose - elimination
if dose > 0
    doses := doses + 1
plot(level)
