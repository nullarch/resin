//@version=5
indicator("Sliding Window")
var window = array.new<float>(0)
array.push(window, close)
if array.size(window) > 5
    array.shift(window)
var int final_size = 0
final_size := array.size(window)
var float result = 0.0
result := array.sum(window)
plot(result)
