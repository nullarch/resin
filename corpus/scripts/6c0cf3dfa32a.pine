
//@version=5
indicator("Risk Adj")
atr = nz(ta.atr(3), 1.0)
rsi = nz(ta.rsi(close, 3), 50.0)
highVol = atr > 2.0
overbought = rsi > 65
riskMult = highVol and overbought ? 0.5 : highVol ? 0.75 : overbought ? 0.8 : 1.0
adjSize = 100 * riskMult
plot(adjSize, "AS")
