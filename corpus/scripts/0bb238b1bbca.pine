
//@version=5
indicator("Harmonic")
a = close
b = ta.sma(close, 3)
c = ta.ema(close, 5)
// Harmonic mean of 3 values: 3/(1/a + 1/b + 1/c)
invSum = (a > 0 ? 1.0/a : 0.0) + (b > 0 ? 1.0/b : 0.0) + (c > 0 ? 1.0/c : 0.0)
harmonic = invSum > 0 ? 3.0 / invSum : 0.0
plot(harmonic, "Harmonic")
