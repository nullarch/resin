
//@version=5
indicator("Consec Trend")
sma3 = ta.sma(close, 3)
count = 0
for i = 0 to 4
    c = close[i]
    m = sma3[i]
    if not na(c) and not na(m) and c > m
        count := count + 1
    else
        break
plot(count, "Cnt")
