
//@version=5
indicator("Stat Wt")
sd = ta.stdev(close, 5)
avgSD = ta.sma(sd, 3)
sdRatio = avgSD > 0 ? sd / avgSD : 1.0
confidence = sdRatio < 0.5 ? 1.0 : sdRatio < 1.0 ? 0.7 : sdRatio < 1.5 ? 0.4 : 0.2
rsi = nz(ta.rsi(close, 3), 50.0)
signal = (rsi - 50) / 50 * confidence
plot(signal, "Sig")
