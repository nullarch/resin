
//@version=5
indicator("TPO Count")
tpoLevels = array.new_float(0)
for i = 0 to 4
    level = low + (high - low) * float(i) / 4.0
    array.push(tpoLevels, level)
tpoCount = array.size(tpoLevels)
plot(float(tpoCount), "TPO")
