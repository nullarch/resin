
//@version=5
indicator("Pair Ratio")
fast = ta.ema(close, 3)
slow = ta.sma(close, 5)
ratio = slow > 0 ? fast / slow : 1.0
ratioMA = ta.sma(ratio, 3)
deviation = ratio - ratioMA
plot(deviation, "Dev")
