
//@version=5
indicator("Signal Registry")
var m = map.new<string, float>()
rsiVal = ta.rsi(close, 3)
sma3 = ta.sma(close, 3)
map.put(m, "rsi", rsiVal)
map.put(m, "sma", sma3)
map.put(m, "price", close)
keys = map.keys(m)
numKeys = array.size(keys)
plot(numKeys, "Keys")
