
//@version=5
indicator("Safe Div")
safeDivide(float a, float b, float fallback) =>
    b != 0 ? a / b : fallback

rng = high - low
bodyPct = safeDivide(math.abs(close - open), rng, 0.0)
volRatio = safeDivide(volume, ta.sma(volume, 5), 1.0)
plot(bodyPct, "BP")
plot(volRatio, "VR")
