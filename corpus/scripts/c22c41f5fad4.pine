
//@version=5
indicator("Convergence")
x = 100.0
target = 50.0
steps = 0
while math.abs(x - target) > 0.01
    x := x * 0.9 + target * 0.1
    steps := steps + 1
    if steps > 100
        break
plot(steps, "Steps")
