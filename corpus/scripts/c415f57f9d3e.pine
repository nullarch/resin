
//@version=5
indicator("UDT Cond Init")
type Signal
    float value = 0.0
    string type_ = "none"
    bool active = false

sma3 = ta.sma(close, 3)
sig = close > sma3 ? Signal.new(close, "buy", true) : Signal.new(close, "sell", false)
plot(sig.active ? 1 : 0, "Active")
