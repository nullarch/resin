
//@version=5
indicator("Converge")
target = ta.sma(close, 3)
est = close
alpha = 0.3
iters = 0
while math.abs(est - target) > 0.01 and iters < 50
    est := est + (target - est) * alpha
    iters := iters + 1
plot(iters, "Iter")
