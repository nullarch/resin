
//@version=5
indicator("Stat Dist")
mahalanobis(float x, float mean, float sd) =>
    sd > 0 ? math.abs(x - mean) / sd : 0.0

mean5 = ta.sma(close, 5)
sd5 = ta.stdev(close, 5)
dist = mahalanobis(close, mean5, sd5)
isOutlier = dist > 2
plot(dist, "Dist")
plot(isOutlier ? 1 : 0, "Out")
