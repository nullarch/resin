
//@version=5
indicator("Nested For")
maxVal = 0.0
for i = 0 to 2
    for j = 0 to 2
        val = close[i] - close[j]
        if not na(val) and val > maxVal
            maxVal := val
plot(maxVal, "Max")
