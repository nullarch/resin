
//@version=5
indicator("RecursiveLike")
factorial(n) =>
    result = 1.0
    for i = 1 to n
        result := result * float(i)
    result
val = math.min(math.floor(high - low), 7)
intVal = val > 0 ? int(val) : 1
fact = factorial(intVal)
plot(math.log(math.max(fact, 1.0)), "LogFact")
