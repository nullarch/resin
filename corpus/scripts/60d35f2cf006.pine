//@version=5
indicator("cross")
fast = ta.sma(close, 3)
slow = ta.sma(close, 5)
var int crossCount = 0
if ta.crossover(fast, slow)
    crossCount := crossCount + 1
if ta.crossunder(fast, slow)
    crossCount := crossCount + 1
