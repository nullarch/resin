
//@version=5
indicator("UDF Z")
zscore(float val, float mean, float sd) =>
    sd > 0 ? (val - mean) / sd : 0.0

sma5 = ta.sma(close, 5)
sd = ta.stdev(close, 5)
z = zscore(close, sma5, sd)
plot(z, "Z")
