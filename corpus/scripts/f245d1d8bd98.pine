
//@version=5
indicator("Osc Norm")
rsi = nz(ta.rsi(close, 3), 50.0)
val = rsi
target = 50.0
iters = 0
while math.abs(val - target) > 0.5 and iters < 30
    val := val + (target - val) * 0.3
    iters := iters + 1
plot(iters, "Its")
