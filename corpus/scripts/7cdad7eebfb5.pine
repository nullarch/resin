//@version=5
indicator("Bar Fingerprint")
fingerprint = (open * 1.0 + high * 2.0 + low * 3.0 + close * 4.0) / 10.0
var float prev_fp = 0.0
fp_changed = math.abs(fingerprint - prev_fp) > 0.01 ? 1.0 : 0.0
prev_fp := fingerprint
