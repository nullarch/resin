
//@version=5
indicator("Vol Regime")
volSMA = ta.sma(volume, 5)
ratio = volSMA > 0 ? volume / volSMA : 1.0
regime = switch
    ratio > 2.5 => "climax"
    ratio > 1.5 => "high"
    ratio > 0.5 => "normal"
    => "dry"
isClimax = regime == "climax"
plot(isClimax ? 1 : 0, "Cli")
