
//@version=5
indicator("Multi Out")
analyze(float src, int len) =>
    ma = ta.sma(src, len)
    dev = ta.stdev(src, len)
    upper = ma + dev
    lower = ma - dev
    [ma, upper, lower]

[mid, up, dn] = analyze(close, 5)
bandwidth = up - dn
plot(bandwidth, "BW")
