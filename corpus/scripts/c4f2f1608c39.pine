//@version=5
indicator("Block Counter")
var int blockCount = 0
var int prevCell = -1
var int blockLen = 0
cell = close > open ? 1 : 0
if cell == prevCell
    blockLen := blockLen + 1
else
    if blockLen >= 2
        blockCount := blockCount + 1
    blockLen := 1
prevCell := cell
