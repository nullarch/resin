//@version=5
indicator("While Convergence")
var float estimate = 100.0
float target = close
float diff = math.abs(estimate - target)
int iter = 0
while diff > 1.0 and iter < 50
    estimate := (estimate + target) / 2.0
    diff := math.abs(estimate - target)
    iter := iter + 1
