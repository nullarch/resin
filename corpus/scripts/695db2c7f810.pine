//@version=5
indicator("ForInDestr")
var float total = 0
var int lastIdx = -1
if bar_index == 0
    arr = array.from(10.0, 20.0, 30.0)
    for [idx, val] in arr
        total := total + val
        lastIdx := idx
