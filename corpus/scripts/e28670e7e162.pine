
//@version=5
indicator("Portfolio")
weights = array.from(0.3, 0.25, 0.2, 0.15, 0.1)
prices = array.from(close, open, high, low, close[1])
total = 0.0
for [i, w] in weights
    p = array.get(prices, i)
    if not na(p)
        total := total + p * w
plot(total, "PortValue")
