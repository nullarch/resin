
//@version=5
indicator("Osc Det")
var int direction = 0
var int changes = 0
sma3 = ta.sma(close, 3)
newDir = close > sma3 ? 1 : -1
if newDir != direction and direction != 0
    changes := changes + 1
direction := newDir
isOscillating = changes > 3
plot(changes, "Chg")
