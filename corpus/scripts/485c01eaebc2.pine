
//@version=5
indicator("Bar Score")
barQuality(float o, float h, float l, float c) =>
    body = math.abs(c - o)
    rng = h - l
    eff = rng > 0 ? body / rng : 0.0
    dir = c > o ? 1.0 : -1.0
    eff * dir

score = barQuality(open, high, low, close)
plot(score, "Score")
