
//@version=5
indicator("Unshift Shift")
var float[] q = array.new_float(0)
array.unshift(q, close)
if array.size(q) > 5
    array.pop(q)
sz = array.size(q)
first = array.first(q)
plot(sz, "Size")
plot(first, "Newest")
