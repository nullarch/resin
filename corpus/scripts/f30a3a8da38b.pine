//@version=5
indicator("Enum UDT Method")
type Indicator
    float value = 0.0
    int var = 0
method update(Indicator this, float newVal) =>
    this.value := newVal
    this.var := this.var + 1
ind = Indicator.new()
ind.update(close)
plot(ind.value)
