
//@version=5
indicator("Map Chain")
var m = map.new<string, float>()
map.put(m, "open", open)
map.put(m, "close", close)
map.put(m, "spread", close - open)
hasSpread = map.contains(m, "spread")
sz = map.size(m)
plot(sz, "Size")
