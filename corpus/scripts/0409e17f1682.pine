//@version=5
indicator("Var multi types")
var float totalFloat = 0.0
var int totalInt = 0
var bool sawBull = false
var string lastTrend = "none"
totalFloat := totalFloat + close
totalInt := totalInt + 1
if close > open and not sawBull
    sawBull := true
lastTrend := close > open ? "bull" : "bear"
