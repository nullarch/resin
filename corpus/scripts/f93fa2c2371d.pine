
//@version=5
indicator("Math NA Safe")
prev = close[5]
safe = nz(prev, close)
logVal = math.log(safe)
sqrtVal = math.sqrt(math.abs(close - safe))
ratio = safe != 0 ? close / safe : 1.0
combined = logVal + sqrtVal + ratio
plot(combined, "Combined")
