
//@version=5
indicator("Stat Scan")
total = 0.0
count = 0
for i = 0 to 4
    if not na(close[i]) and not na(close[i+1])
        ret = (close[i] - close[i+1]) / close[i+1] * 100
        total := total + ret
        count := count + 1
avgRet = count > 0 ? total / count : 0.0
plot(avgRet, "AR")
