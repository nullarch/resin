
//@version=5
indicator("Regime Wt")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
spread = sma5 > 0 ? (sma3 - sma5) / sma5 * 100 : 0.0
weight = math.abs(spread) > 2 ? 1.5 : math.abs(spread) > 0.5 ? 1.0 : 0.5
rsi = nz(ta.rsi(close, 3), 50.0)
signal = (rsi - 50) / 50 * weight
plot(signal, "Sig")
