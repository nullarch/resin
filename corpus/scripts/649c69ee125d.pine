//@version=5
indicator("PopInit")
pop = array.new_float(0)
for i = 0 to 7
    gene = math.sin(float(i) * close / 100.0) * 50.0 + 50.0
    array.push(pop, gene)
bestGene = array.max(pop)
worstGene = array.min(pop)
spread = bestGene - worstGene
plot(spread, "Spread")
