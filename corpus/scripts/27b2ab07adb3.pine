
//@version=5
indicator("Normalize")
normalize(float val, float minVal, float maxVal) =>
    range = maxVal - minVal
    range > 0 ? (val - minVal) / range : 0.5

hi5 = ta.highest(high, 5)
lo5 = ta.lowest(low, 5)
norm = normalize(close, lo5, hi5)
plot(norm, "Norm")
