
//@version=5
indicator("Nest Break")
sma3 = ta.sma(close, 3)
found = false
foundBar = -1
for i = 0 to 5
    if na(close[i]) or na(sma3[i])
        continue
    if close[i] > sma3[i] * 1.02
        foundBar := i
        found := true
        break
plot(foundBar, "FB")
