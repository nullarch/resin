//@version=5
indicator("While break")
var int found = -1
i = 0
arr = array.from(10.0, 20.0, 30.0, 40.0, 50.0)
while i < array.size(arr)
    if array.get(arr, i) >= 30.0
        found := i
        break
    i := i + 1
