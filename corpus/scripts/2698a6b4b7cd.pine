//@version=5
indicator("While Converge")
float guess = 50.0
float while_val = while math.abs(guess - 25.0) > 0.1
    guess := (guess + 25.0) / 2.0
    guess
var float result = 0.0
result := while_val
plot(result)
