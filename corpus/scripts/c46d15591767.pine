
//@version=5
indicator("MR UDF")
mrScore(float price, float mean, float sd) =>
    z = sd > 0 ? (price - mean) / sd : 0.0
    z < -2 ? 1.0 : z < -1 ? 0.5 : z > 2 ? -1.0 : z > 1 ? -0.5 : 0.0

mean5 = ta.sma(close, 5)
sd5 = ta.stdev(close, 5)
score = mrScore(close, mean5, sd5)
plot(score, "MR")
