
//@version=5
indicator("Tanh")
tanhApprox(x) =>
    clamped = math.max(math.min(x, 5.0), -5.0)
    ep = math.exp(2.0 * clamped)
    (ep - 1.0) / (ep + 1.0)
norm = (close - 105.0) / 5.0
out = tanhApprox(norm)
plot(out, "Tanh")
