
//@version=5
indicator("Band Scan")
sma5 = ta.sma(close, 5)
stdev = nz(ta.stdev(close, 5), 1.0)
aboveUpper = 0
for i = 0 to 4
    if not na(close[i])
        upper = sma5 + stdev * 2
        if close[i] > upper
            aboveUpper := aboveUpper + 1
plot(aboveUpper, "AU")
