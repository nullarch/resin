//@version=5
indicator("state machine")
var int state = 0
var float result = 0.0
if state == 0
    if close > open
        state := 1
else if state == 1
    if close < open
        state := 0
result := state
