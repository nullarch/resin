
//@version=5
indicator("Map Accum")
var acc = map.new<string, float>()
prevSum = map.get(acc, "sum", 0.0)
prevCnt = map.get(acc, "count", 0.0)
map.put(acc, "sum", prevSum + close)
map.put(acc, "count", prevCnt + 1)
runAvg = (prevSum + close) / (prevCnt + 1)
plot(runAvg, "Avg")
