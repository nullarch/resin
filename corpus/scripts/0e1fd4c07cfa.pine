//@version=5
indicator("Input Bool Str")
enabled = input.bool(true, title="Enable")
label_ = input.string("SMA", title="Label")
var float result = 0.0
result := enabled ? close : 0.0
var int lbl_len = 0
lbl_len := str.length(label_)
plot(close)
