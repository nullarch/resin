
//@version=5
indicator("Skip Invalid")
total = 0.0
count = 0
for i = 0 to 9
    val = close[i]
    if na(val)
        continue
    if val < 100
        continue
    total := total + val
    count := count + 1
avg = count > 0 ? total / count : 0.0
plot(avg, "Avg")
