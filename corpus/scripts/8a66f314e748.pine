
//@version=5
indicator("Mom Streak")
var int streak = 0
rsi = nz(ta.rsi(close, 3), 50.0)
if rsi > 50
    streak := streak > 0 ? streak + 1 : 1
else
    streak := streak < 0 ? streak - 1 : -1
plot(streak, "Str")
