
//@version=5
indicator("OBV Ext")
var float obv = 0.0
if close > close[1] and not na(close[1])
    obv := obv + volume
else if close < close[1] and not na(close[1])
    obv := obv - volume
obvMA = ta.sma(obv, 3)
diverging = (close > close[1] and obv < obv[1]) or (close < close[1] and obv > obv[1])
plot(obv, "OBV")
plot(diverging ? 1 : 0, "Div")
