
//@version=5
indicator("Consec")
bullCount = 0
for i = 0 to 4
    if not na(close[i]) and not na(open_[i])
        if close[i] > open_[i]
            bullCount := bullCount + 1
        else
            bullCount := 0
plot(bullCount, "BC")
