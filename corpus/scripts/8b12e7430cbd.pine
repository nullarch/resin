
//@version=5
indicator("Softmax2")
z1 = (close - open) / 2.0
z2 = (high - low) / 2.0
clamped1 = math.max(math.min(z1, 10.0), -10.0)
clamped2 = math.max(math.min(z2, 10.0), -10.0)
e1 = math.exp(clamped1)
e2 = math.exp(clamped2)
sumE = e1 + e2
p1 = sumE > 0 ? e1 / sumE : 0.5
plot(p1, "Prob1")
