//@version=5
indicator("Str ReplaceAll")
var string s = "aabbaabb"
var string result = ""
result := str.replace_all(s, "aa", "XX")
var int len_ = 0
len_ := str.length(result)
var bool has_xx = false
has_xx := str.contains(result, "XX")
plot(close)
