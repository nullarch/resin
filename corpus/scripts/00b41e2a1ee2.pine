//@version=5
strategy("PID Strategy")
float setpoint = 106.0
float err = setpoint - close
var float integral = 0.0
integral := integral + 0.05 * err
float pidOut = 0.5 * err + integral
if pidOut > 2.0
    strategy.entry("Long", strategy.long)
if pidOut < -2.0
    strategy.close("Long")
