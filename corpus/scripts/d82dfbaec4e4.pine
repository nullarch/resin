//@version=5
indicator("Chain Array")
var float[] arr = array.new<float>(0)
arr.push(close)
if arr.size() > 5
    arr.shift()
sorted = arr.copy()
sorted.sort()
mid = sorted.size() > 0 ? sorted.get(math.floor(sorted.size() / 2)) : 0.0
plot(mid)
