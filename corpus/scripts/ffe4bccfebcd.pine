//@version=5
strategy("OU Reversion")
float mu = ta.sma(close, 5)
float sigma = ta.stdev(close, 5)
float zScore = sigma > 0.01 ? (close - mu) / sigma : 0.0
if zScore < -1.0
    strategy.entry("MeanRev", strategy.long)
if zScore > 1.0
    strategy.close("MeanRev")
