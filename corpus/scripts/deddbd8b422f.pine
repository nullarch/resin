
//@version=5
indicator("TPO Shape")
range_ = high - low
body = math.abs(close - open)
ratio = range_ > 0 ? body / range_ : 0.0
shape = switch
    ratio > 0.7 => 3
    ratio > 0.4 => 2
    => 1
plot(float(shape), "Shape")
