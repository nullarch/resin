
//@version=5
strategy("Close All", overlay=true)
sma3 = ta.sma(close, 3)
rsiVal = ta.rsi(close, 3)
if ta.crossover(close, sma3)
    strategy.entry("Long", strategy.long)
if rsiVal > 70
    strategy.close_all()
plot(rsiVal, "RSI")
