
//@version=5
indicator("VP Analysis")
avgVol = ta.sma(volume, 5)
highVol = volume > avgVol * 1.5
priceUp = close > open
volPriceConfirm = highVol and priceUp
cumConfirm = ta.cum(volPriceConfirm ? 1 : 0)
plot(cumConfirm, "Cum")
