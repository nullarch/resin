
//@version=5
indicator("Time Lbl")
var int barNum = 0
barNum := barNum + 1
pos = barNum % 5
period = pos <= 1 ? "EARLY" : pos <= 3 ? "MID" : "LATE"
trend = close > ta.sma(close, 3) ? "UP" : "DN"
label = period + "_" + trend
isEarlyUp = label == "EARLY_UP"
plot(isEarlyUp ? 1 : 0, "EU")
