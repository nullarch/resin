
//@version=5
indicator("Risk Trade")
capital = 10000.0
riskPct = 2.0
atr = ta.atr(3)
stopDist = atr * 2
riskAmt = capital * riskPct / 100
shares = stopDist > 0 ? math.floor(riskAmt / stopDist) : 0
plot(shares, "Shares")
