
//@version=5
indicator("Risk Level")
atr = ta.atr(3)
avgATR = ta.sma(atr, 5)
rsi = nz(ta.rsi(close, 3), 50.0)
volHigh = atr > avgATR * 1.5
momExtreme = rsi > 75 or rsi < 25
riskLevel = switch
    volHigh and momExtreme => 3
    volHigh or momExtreme => 2
    => 1
plot(riskLevel, "Risk")
