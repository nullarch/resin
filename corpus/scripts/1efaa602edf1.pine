
//@version=5
indicator("ATR%")
atr = ta.atr(3)
atrPct = close > 0 ? atr / close * 100 : 0.0
avgAtrPct = ta.sma(atrPct, 5)
isHighVol = atrPct > avgAtrPct * 1.5
plot(atrPct, "ATR%")
plot(isHighVol ? 1 : 0, "HiVol")
