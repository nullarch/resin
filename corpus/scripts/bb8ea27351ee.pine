
//@version=5
indicator("RAR")
riskAdjReturn(float ret, float vol, float riskFree) =>
    vol > 0 ? (ret - riskFree) / vol : 0.0

retVal = close - close[1]
vol = ta.stdev(retVal, 5)
rar = riskAdjReturn(nz(retVal, 0.0), vol, 0.0)
plot(rar, "RAR")
