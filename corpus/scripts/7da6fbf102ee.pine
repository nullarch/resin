//@version=5
indicator("UDT_TA")
type Signal
    float rsi = 0.0
    float sma = 0.0
    bool valid = false
var Signal sig = Signal.new()
rsiVal = ta.rsi(close, 5)
smaVal = ta.sma(close, 3)
if not na(rsiVal) and not na(smaVal)
    sig.rsi := rsiVal
    sig.sma := smaVal
    sig.valid := true
