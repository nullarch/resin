//@version=5
indicator("test")
getMA(float src, int len) =>
    ta.sma(src, len)

getSignal(float src) =>
    short = getMA(src, 3)
    long = getMA(src, 5)
    short - long

sig = getSignal(close)
plot(sig)
