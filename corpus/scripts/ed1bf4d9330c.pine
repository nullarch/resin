
//@version=5
indicator("Pivot")
prevH = high[1]
prevL = low[1]
prevC = close[1]
pivot = not na(prevH) ? (prevH + prevL + prevC) / 3 : close
r1 = 2 * pivot - (not na(prevL) ? prevL : low)
s1 = 2 * pivot - (not na(prevH) ? prevH : high)
plot(pivot, "PP")
plot(r1, "R1")
plot(s1, "S1")
