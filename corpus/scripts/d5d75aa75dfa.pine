//@version=5
indicator("Var Track")
var float maxClose = na
var float minClose = na
if na(maxClose) or close > maxClose
    maxClose := close
if na(minClose) or close < minClose
    minClose := close
range_ = maxClose - minClose
pct = range_ / maxClose * 100
plot(pct)
