
//@version=5
indicator("Vol Bucket")
atr = ta.atr(3)
avgATR = ta.sma(atr, 5)
ratio = avgATR > 0 ? atr / avgATR : 1.0
bucket = switch
    ratio > 3.0 => 5
    ratio > 2.0 => 4
    ratio > 1.5 => 3
    ratio > 1.0 => 2
    ratio > 0.5 => 1
    => 0
plot(bucket, "Bucket")
