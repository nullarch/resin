//@version=5
strategy("DFA Signal Strategy")
var int state = 0
bull = close > open
state := state == 0 and bull ? 1 : state == 1 and bull ? 2 : state == 2 and not bull ? 0 : state
if state == 2
    strategy.entry("DFALong", strategy.long)
if state == 0
    strategy.close("DFALong")
