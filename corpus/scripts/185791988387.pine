//@version=5
indicator("DG-03 Signed Curvature")
var float signedK = 0.0
if not na(close[2])
    dp1 = close - close[1]
    dp0 = close[1] - close[2]
    d2p = dp1 - dp0
    denom = math.pow(1.0 + dp1 * dp1, 1.5)
    signedK := denom > 0.001 ? d2p / denom : 0.0
plot(signedK)
