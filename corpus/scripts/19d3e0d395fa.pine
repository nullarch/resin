//@version=5
indicator("MACD Verify")
[macdLine, signalLine, hist] = ta.macd(close, 3, 5, 3)
computed_hist = macdLine - signalLine
diff = na(hist) ? 0.0 : math.abs(hist - computed_hist)
small = diff < 0.0001
plot(small ? 1.0 : 0.0)
