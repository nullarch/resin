
//@version=5
indicator("Iter Smooth")
val = close
alpha = 0.3
target = ta.sma(close, 5)
iters = 0
while iters < 8
    val := val * (1 - alpha) + target * alpha
    iters := iters + 1
plot(val, "Smoothed")
