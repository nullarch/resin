
//@version=5
indicator("Spread Adj")
barRange = high - low
avgRange = ta.sma(high - low, 5)
ratio = avgRange > 0 ? barRange / avgRange : 1.0
adj = ratio > 2.0 ? 0.5 : ratio > 1.5 ? 0.75 : ratio < 0.5 ? 1.5 : 1.0
rsi = nz(ta.rsi(close, 3), 50.0)
signal = (rsi - 50) / 50 * adj
plot(signal, "Sig")
