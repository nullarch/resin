
//@version=5
indicator("Map Portfolio")
var weights = map.new<string, float>()
map.put(weights, "asset1", 0.5)
map.put(weights, "asset2", 0.3)
map.put(weights, "asset3", 0.2)
total = 0.0
for [key, wt] in weights
    total := total + wt * close
plot(total, "Portfolio")
