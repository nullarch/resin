//@version=5
indicator("Method UDT")
type Pair
    float a
    float b

method avg_val(Pair p) =>
    (p.a + p.b) / 2.0

p = Pair.new(10.0, 30.0)
var float result = 0.0
result := p.avg_val()
plot(result)
