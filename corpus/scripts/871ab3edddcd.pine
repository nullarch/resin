//@version=5
indicator("UDT Array")
type Entry
    float price
    int idx

var entries = array.new<Entry>()
if bar_index % 3 == 0
    e = Entry.new(close, bar_index)
    array.push(entries, e)
var int count = 0
count := array.size(entries)
plot(count)
