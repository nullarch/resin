//@version=5
indicator("Colimit")
float sma3 = nz(ta.sma(close, 3), close)
float sma5 = nz(ta.sma(close, 5), close)
float ema3 = nz(ta.ema(close, 3), close)
float colimitVal = math.max(sma3, math.max(sma5, ema3))
float spread = colimitVal - math.min(sma3, math.min(sma5, ema3))
