
//@version=5
indicator("Roll ZScore")
var float[] vals = array.new_float(0)
array.push(vals, close)
if array.size(vals) > 5
    array.shift(vals)
avg = array.avg(vals)
sd = array.stdev(vals)
zscore = sd > 0 ? (close - avg) / sd : 0.0
plot(zscore, "Z")
