
//@version=5
indicator("Gradient")
var float gradMag = 0.0
prevC = close[1]
if not na(prevC) and prevC > 0
    grad = (close - prevC) / prevC
    gradMag := math.abs(grad)
else
    gradMag := 0.0
plot(gradMag, "Grad")
