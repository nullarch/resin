
//@version=5
indicator("Vol Scale")
atr = nz(ta.atr(3), 1.0)
avgATR = ta.sma(nz(ta.atr(3), 1.0), 5)
ratio = avgATR > 0 ? atr / avgATR : 1.0
scale = ratio > 1.5 ? 0.5 : ratio > 1.0 ? 1.0 : 1.5
rsi = nz(ta.rsi(close, 3), 50.0)
signal = (rsi - 50) * scale
plot(signal, "Sig")
