
//@version=5
indicator("Noise Red")
raw = close - ta.sma(close, 3)
smoothed = raw
iters = 0
while math.abs(smoothed) > 0.1 and iters < 10
    smoothed := smoothed * 0.7
    iters := iters + 1
plot(iters, "It")
