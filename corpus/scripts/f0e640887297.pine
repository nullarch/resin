//@version=5
indicator("Replication")
replica1 = ta.ema(close, 3)
replica2 = ta.ema(close, 5)
replica3 = ta.ema(close, 7)
avg_replica = not na(replica1) and not na(replica2) and not na(replica3) ? (replica1 + replica2 + replica3) / 3.0 : close
