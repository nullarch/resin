
//@version=5
indicator("Period Scan")
sma3 = ta.sma(close, 3)
bullBars = 0
for i = 0 to 4
    if not na(close[i])
        above = close[i] > sma3
        if above
            bullBars := bullBars + 1
consistency = bullBars / 5.0 * 100
plot(consistency, "Con")
