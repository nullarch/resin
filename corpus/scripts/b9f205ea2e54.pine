//@version=5
indicator("Map overwrite")
var m = map.new<string, float>()
map.put(m, "price", close)
map.put(m, "high", high)
map.put(m, "low", low)
map.put(m, "price", close * 2)
curPrice = map.get(m, "price")
sz = map.size(m)
