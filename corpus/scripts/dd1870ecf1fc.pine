
//@version=5
indicator("Enum Score")
enum Strength
    Strong = 3
    Medium = 2
    Weak = 1
    None_ = 0

rsiVal = ta.rsi(close, 3)
s = rsiVal > 70 ? Strength.Strong : rsiVal > 60 ? Strength.Medium : rsiVal > 50 ? Strength.Weak : Strength.None_
plot(int(s), "Strength")
