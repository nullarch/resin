
//@version=5
indicator("Perceptron")
sigmoid(x) =>
    1.0 / (1.0 + math.exp(-math.max(math.min(x, 10.0), -10.0)))
w1 = 0.5
w2 = -0.3
bias = 0.1
normClose = (close - 100.0) / 10.0
normVol = (volume - 1000.0) / 500.0
z = w1 * normClose + w2 * normVol + bias
out = sigmoid(z)
plot(out, "Perceptron")
