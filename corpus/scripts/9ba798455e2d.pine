//@version=5
indicator("UDT Method")
type Trade
    float entry = 0.0
    float target = 0.0
    int bars = 0
newTrade(float price, float atr) =>
    Trade.new(price, price + atr * 2, 0)
atrVal = ta.atr(high, low, close, 5)
t = newTrade(close, atrVal)
plot(t.target)
