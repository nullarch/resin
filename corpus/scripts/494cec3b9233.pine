
//@version=5
indicator("Bars Since")
var int barsSince = 0
sma3 = ta.sma(close, 3)
crossed = ta.crossover(close, sma3)
if crossed
    barsSince := 0
else
    barsSince := barsSince + 1
plot(barsSince, "BS")
