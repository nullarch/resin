
//@version=5
indicator("Entry Exit")
ema3 = ta.ema(close, 3)
sma5 = ta.sma(close, 5)
rsi = nz(ta.rsi(close, 3), 50.0)
entry = ta.crossover(ema3, sma5) and rsi > 45
exit = ta.crossunder(ema3, sma5) or rsi > 80
plot(entry ? 1 : exit ? -1 : 0, "Signal")
