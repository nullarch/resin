//@version=5
indicator("ForInEnt16")
probs = array.new_float(0)
array.push(probs, 0.3)
array.push(probs, 0.5)
array.push(probs, 0.2)
ent = 0.0
for [idx, p] in probs
    if p > 0.001
        ent := ent - p * math.log(p)
plot(ent, "e")
