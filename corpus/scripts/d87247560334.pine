//@version=5
indicator("test")
calcBands(src, len) =>
    basis = ta.sma(src, len)
    dev = ta.stdev(src, len)
    upper = basis + 2 * dev
    lower = basis - 2 * dev
    [upper, lower]
[bbUpper, bbLower] = calcBands(close, 5)
plot(bbUpper)
