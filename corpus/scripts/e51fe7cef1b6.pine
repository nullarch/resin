
//@version=5
indicator("MR Bands")
sma5 = ta.sma(close, 5)
dev = ta.stdev(close, 5)
upper = sma5 + nz(dev, 0.0) * 1.5
lower = sma5 - nz(dev, 0.0) * 1.5
aboveUpper = close > upper
belowLower = close < lower
plot(upper, "Up")
plot(lower, "Lo")
