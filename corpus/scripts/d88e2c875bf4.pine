
//@version=5
indicator("Adapt Period")
adaptiveMA(float src, float volatility) =>
    period = volatility > 2.0 ? 3 : volatility > 1.0 ? 5 : 8
    ta.sma(src, period)

atr = ta.atr(3)
adaptMA = adaptiveMA(close, atr)
plot(adaptMA, "AdaptMA")
