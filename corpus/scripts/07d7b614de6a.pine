//@version=5
indicator("PutCallRatio")
var int bullBars = 0
var int bearBars = 0
if close > open
    bullBars := bullBars + 1
else
    bearBars := bearBars + 1
ratio = bullBars > 0 ? float(bearBars) / float(bullBars) : 1.0
plot(ratio, "PCR")
