//@version=5
indicator("test")
type Signal
    float value = 0.0
    bool active = false
    string label = ""

method activate(Signal this) =>
    this.active := true
    this

s = Signal.new(close, false, "test")
s.activate()
plot(s.active ? 1 : 0)
