
//@version=5
indicator("Algo Param")
atr = nz(ta.atr(3), 1.0)
avgATR = ta.sma(nz(ta.atr(3), 1.0), 5)
ratio = avgATR > 0 ? atr / avgATR : 1.0
alpha = ratio > 1.5 ? 0.8 : ratio > 1.0 ? 0.5 : 0.3
var float smoothed = 0.0
smoothed := alpha * close + (1 - alpha) * smoothed
plot(close - smoothed, "D")
