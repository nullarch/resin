
//@version=5
indicator("Vol Reg")
atr = nz(ta.atr(3), 1.0)
avgATR = ta.sma(nz(ta.atr(3), 1.0), 5)
ratio = avgATR > 0 ? atr / avgATR : 1.0
regime = ratio > 1.5 ? 3 : ratio > 1.2 ? 2 : ratio < 0.7 ? 0 : 1
plot(regime, "R")
