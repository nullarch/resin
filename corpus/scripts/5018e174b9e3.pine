
//@version=5
indicator("Time Data")
var td = map.new<string, float>()
var int barNum = 0
barNum := barNum + 1
map.put(td, "bar", float(barNum))
map.put(td, "period", float(barNum % 5))
map.put(td, "close", close)
period = map.get(td, "period", 0.0)
plot(period, "Per")
