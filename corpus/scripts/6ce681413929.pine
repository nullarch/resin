
//@version=5
indicator("Sliding Stats")
var float[] win = array.new_float(0)
array.push(win, close)
if array.size(win) > 5
    array.shift(win)
sz = array.size(win)
avg = array.avg(win)
mx = array.max(win)
mn = array.min(win)
rng = mx - mn
plot(rng, "Range")
