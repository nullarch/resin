//@version=5
indicator("Tail Call Proxy")
float val = close
float result = 0.0
for i = 0 to 7
    result := result + val
    val := val * 0.5
float geometric = close * (1.0 - math.pow(0.5, 8.0)) / (1.0 - 0.5)
float error = math.abs(result - geometric)
