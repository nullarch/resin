//@version=5
indicator("Shared Get")
arr = array.new<float>(0)
array.push(arr, 42.0)
float arr_val = array.get(arr, 0)

m = map.new<string, float>()
map.put(m, "k", 99.0)
float map_val = map.get(m, "k")

var float result = 0.0
result := arr_val + map_val
plot(result)
