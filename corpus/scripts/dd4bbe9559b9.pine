//@version=5
indicator("Call ID Isolation")
a = ta.sma(close, 3)
b = ta.sma(close, 3)
// Both should produce identical results
var float last_a = 0.0
var float last_b = 0.0
last_a := nz(a, 0.0)
last_b := nz(b, 0.0)
var float result = 0.0
result := last_a - last_b
plot(result)
