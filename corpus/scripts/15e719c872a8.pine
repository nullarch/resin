//@version=5
indicator("Map Remove")
m = map.new<string, float>()
map.put(m, "a", 10.0)
map.put(m, "b", 20.0)
map.put(m, "c", 30.0)
map.remove(m, "b")
var float result = 0.0
result := float(map.size(m))
plot(result)
