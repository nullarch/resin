
//@version=5
indicator("Bull Engulf")
prevBear = close[1] < open[1]
currBull = close > open
engulf = not na(close[1]) and prevBear and currBull and close > open[1] and open < close[1]
plot(engulf ? 1 : 0, "BE")
