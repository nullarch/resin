//@version=5
indicator("ATR Trail")
atrVal = ta.atr(high, low, close, 5)
var float trailStop = 0.0
newStop = close - atrVal * 2
if close > trailStop
    trailStop := math.max(trailStop, newStop)
else
    trailStop := newStop
plot(trailStop)
