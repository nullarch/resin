//@version=5
indicator("Enum UDT")
enum State
    Bullish
    Bearish
    Neutral
type MarketState
    float price = 0.0
    int trend = 0
ms = MarketState.new(close, close > open ? 1 : -1)
plot(ms.trend)
