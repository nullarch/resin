//@version=5
indicator("Large array")
var arr = array.new_float(0)
for i = 0 to 99
    array.push(arr, float(i))
array.sort(arr)
sz = array.size(arr)
mn = array.min(arr)
mx = array.max(arr)
sm = array.sum(arr)
avg_val = array.avg(arr)
