//@version=5
indicator("Freq Modulation")
float emaFast = nz(ta.ema(close, 2), close)
float deviation = close - emaFast
float prevClose = not na(close[1]) ? close[1] : close
float prevDev = prevClose - emaFast
float freqMod = deviation - prevDev
plot(freqMod, "FreqMod")
