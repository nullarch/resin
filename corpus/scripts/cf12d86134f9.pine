
//@version=5
indicator("RR Ratio")
atr = ta.atr(3)
sma3 = ta.sma(close, 3)
entry = close
stop = entry - atr * 2
target = entry + atr * 3
risk = entry - stop
reward = target - entry
rr = risk > 0 ? reward / risk : 0.0
plot(rr, "RR")
