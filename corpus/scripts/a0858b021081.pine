
//@version=5
indicator("Factor Scan")
sma3 = ta.sma(close, 3)
aboveMAbars = 0
for i = 0 to 4
    if not na(close[i])
        if close[i] > sma3
            aboveMAbars := aboveMAbars + 1
factorStr = aboveMAbars / 5.0 * 100
plot(factorStr, "FS")
