
//@version=5
indicator("Switch Calc")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
diff = sma3 - sma5
signal = switch
    diff > 2 => 2
    diff > 0 => 1
    diff < -2 => -2
    diff < 0 => -1
    => 0
plot(signal, "Signal")
