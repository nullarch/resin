//@version=5
indicator("ArrSortTA")
var a = array.new<float>(0)
var float medianVal = 0
emaVal = ta.ema(close, 3)
if not na(emaVal) and bar_index < 5
    array.push(a, emaVal)
if bar_index == 9 and array.size(a) > 0
    array.sort(a)
    medianVal := array.get(a, int(array.size(a) / 2))
