
//@version=5
indicator("Vol Lbl")
avgVol = ta.sma(volume, 5)
ratio = avgVol > 0 ? volume / avgVol : 1.0
volLbl = ratio > 1.5 ? "HIGH" : ratio < 0.7 ? "LOW" : "NORM"
priceLbl = close > close[1] ? "UP" : "DN"
label = volLbl + "_" + priceLbl
isHighUp = label == "HIGH_UP"
plot(isHighUp ? 1 : 0, "HU")
