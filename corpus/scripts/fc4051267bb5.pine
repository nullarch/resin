//@version=5
indicator("Volume Profile")
var float totalVol = 0.0
var float upVol = 0.0
var float downVol = 0.0
totalVol := totalVol + volume
if close > open
    upVol := upVol + volume
else
    downVol := downVol + volume
volRatio = totalVol > 0 ? upVol / totalVol : 0.5
