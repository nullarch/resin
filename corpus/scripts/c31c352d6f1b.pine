//@version=5
indicator("Comprehensive C97")
var float result = 0.0
lbl = label.new(bar_index, close)
myMA(src, len) =>
    if len > 0
        ta.sma(src, len)
    else
        src
result := myMA(close, 3)
