
//@version=5
indicator("Vol Thresh")
avgVol = ta.sma(volume, 5)
threshold = avgVol * 3
steps = 0
while threshold > volume and steps < 15
    threshold := threshold * 0.7
    steps := steps + 1
plot(steps, "St")
