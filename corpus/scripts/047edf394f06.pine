//@version=5
indicator("DualState06")
var float pos = 100.0
var float vel = 0.0
kp = 0.4
kv = 0.1
predPos = pos + vel
residual = close - predPos
pos := predPos + kp * residual
vel := vel + kv * residual
spread = math.abs(close - pos)
plot(pos, "pos")
plot(spread, "spr")
