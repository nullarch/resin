
//@version=5
indicator("MA Filt")
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
aligned = sma3 > sma5
rsi = nz(ta.rsi(close, 3), 50.0)
signal = aligned ? rsi > 55 ? 2.0 : 1.0 : rsi < 45 ? -2.0 : -1.0
plot(signal, "Sig")
