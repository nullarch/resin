
//@version=5
indicator("Dropout")
inp = (close - 100.0) / 10.0
dropRate = 3
kept = bar_index % dropRate != 0 ? inp : 0.0
scale = dropRate > 1 ? float(dropRate) / float(dropRate - 1) : 1.0
out = kept * scale
plot(out, "Dropout")
