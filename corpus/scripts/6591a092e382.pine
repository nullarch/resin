
//@version=5
indicator("Trend Score")
trendScore(float price, float ma, float atr) =>
    dist = price - ma
    atr > 0 ? dist / atr : 0.0

sma5 = ta.sma(close, 5)
atr = ta.atr(3)
score = trendScore(close, sma5, atr)
plot(score, "TS")
