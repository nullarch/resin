//@version=5
indicator("C300-05")
var int mode = 0
mode := (mode + 1) % 3
smaVal = ta.sma(close, 3)
emaVal = ta.ema(close, 3)
rsiVal = nz(ta.rsi(close, 3), 50.0)
result = switch mode
    0 => nz(smaVal, close)
    1 => nz(emaVal, close)
    => rsiVal
plot(result)
