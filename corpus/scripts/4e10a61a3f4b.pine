
//@version=5
indicator("Hi Since")
hi5 = ta.highest(high, 5)
lo5 = ta.lowest(low, 5)
midLine = (hi5 + lo5) / 2.0
distFromHi = hi5 - close
distPct = hi5 > 0 ? distFromHi / hi5 * 100 : 0.0
plot(distPct, "DistPct")
