//@version=5
indicator("Method Expr Return")
var float result = 0.0

type Vec
    float x = 0.0
    float y = 0.0

method magnitude(Vec self) =>
    math.sqrt(self.x * self.x + self.y * self.y)

v = Vec.new(3.0, 4.0)
result := v.magnitude()
