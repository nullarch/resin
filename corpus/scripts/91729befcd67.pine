//@version=5
indicator("MM1 Queue")
float volRatio = ta.sma(volume, 3)
float maxVol = ta.highest(volume, 5)
float rho = nz(maxVol, 1.0) > 0 ? nz(volRatio, 0.0) / nz(maxVol, 1.0) : 0.0
float safeRho = math.min(rho, 0.95)
float mm1L = safeRho / (1.0 - safeRho + 0.001)
plot(mm1L, "MM1_L")
