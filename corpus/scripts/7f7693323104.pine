//@version=5
indicator("Method Decl")
type Counter
    float count

method increment(Counter c) =>
    c.count := c.count + 1.0
    c.count

ctr = Counter.new(0.0)
var float result = 0.0
result := ctr.increment()
plot(result)
