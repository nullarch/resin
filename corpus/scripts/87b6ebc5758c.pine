
//@version=5
indicator("Trend Quality")
trendQuality(float price, float ma, float sd) =>
    dist = math.abs(price - ma)
    sd > 0 ? dist / sd : 0.0

sma5 = ta.sma(close, 5)
sd = ta.stdev(close, 5)
tq = trendQuality(close, sma5, sd)
plot(tq, "TQ")
