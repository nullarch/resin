
//@version=5
strategy("Math Size", overlay=true)
atr = ta.atr(3)
sma3 = ta.sma(close, 3)
riskPerUnit = atr * 2
posSize = riskPerUnit > 0 ? math.floor(1000 / riskPerUnit) : 1
if close > sma3
    strategy.entry("Long", strategy.long)
if close < sma3
    strategy.close("Long")
plot(sma3, "MA")
