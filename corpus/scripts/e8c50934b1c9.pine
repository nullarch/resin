
//@version=5
indicator("Donchian")
hi = high
lo = low
for i = 1 to 4
    h = high[i]
    l = low[i]
    if not na(h) and h > hi
        hi := h
    if not na(l) and l < lo
        lo := l
mid = (hi + lo) / 2.0
width = hi - lo
plot(mid, "Mid")
plot(width, "Width")
