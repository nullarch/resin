
//@version=5
indicator("State Machine")
var int state = 0
rsi = nz(ta.rsi(close, 3), 50.0)
if state == 0 and rsi > 60
    state := 1
else if state == 1 and rsi < 40
    state := 2
else if state == 2
    state := 0
plot(state, "State")
