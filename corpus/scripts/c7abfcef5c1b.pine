//@version=5
indicator("ForAvg")
var float myAvg = 0
sum = 0.0
count = 0
for i = 0 to 2
    val = close[i]
    if not na(val)
        sum := sum + val
        count := count + 1
if count > 0
    myAvg := sum / count
