
//@version=5
indicator("Converge")
estimate = close
target = ta.sma(close, 3)
iterations = 0
while math.abs(estimate - target) > 0.01 and iterations < 20
    estimate := (estimate + target) / 2.0
    iterations := iterations + 1
plot(estimate, "Est")
plot(iterations, "Iters")
