//@version=5
indicator("for in map")
var float result = 0.0
m = map.new<string, float>()
map.put(m, "x", 10.0)
map.put(m, "y", 20.0)
map.put(m, "z", 30.0)
float total = 0.0
for [k, v] in m
    total := total + v
result := total
