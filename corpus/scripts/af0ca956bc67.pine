
//@version=5
indicator("BuildConsume")
arr = array.new_float(0)
for i = 0 to 4
    array.push(arr, close + float(i))
total = 0.0
idx = 0
sz = array.size(arr)
while idx < sz
    total := total + array.get(arr, idx)
    idx := idx + 1
avg = sz > 0 ? total / float(sz) : 0.0
plot(avg, "A")
