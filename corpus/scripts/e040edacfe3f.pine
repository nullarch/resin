
//@version=5
indicator("Concat")
a = array.from(close, open)
b = array.from(high, low)
merged = array.concat(a, b)
total = array.sum(merged)
sz = array.size(merged)
plot(total, "Total")
plot(sz, "Size")
