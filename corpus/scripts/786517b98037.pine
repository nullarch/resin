//@version=5
indicator("test")
trend = ta.ema(close, 3) > ta.sma(close, 5)
var int score = 0
if trend
    score := switch
        close > high[1] => 3
        close > open => 2
        => 1
else
    score := switch
        close < low[1] => -3
        close < open => -2
        => -1
plot(score)
