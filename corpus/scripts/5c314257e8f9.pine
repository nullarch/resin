
//@version=5
indicator("Score Opt")
rsi = nz(ta.rsi(close, 3), 50.0)
threshold = 80.0
matches = 0
while threshold > 40 and matches < 3
    if rsi < threshold
        matches := matches + 1
    threshold := threshold - 10
plot(matches, "M")
