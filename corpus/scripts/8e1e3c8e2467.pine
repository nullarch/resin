
//@version=5
indicator("TF Sim")
var int barIdx = 0
barIdx := barIdx + 1
phase = barIdx % 4
action = switch phase
    0 => "accumulate"
    1 => "markup"
    2 => "distribute"
    => "markdown"
isAccum = action == "accumulate"
plot(isAccum ? 1 : 0, "Accum")
