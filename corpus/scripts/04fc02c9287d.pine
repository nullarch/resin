//@version=5
indicator("Switch Complex")
sma3 = ta.sma(close, 3)
rsi_ = ta.rsi(close, 5)
result = switch
    rsi_ > 70 => "overbought"
    rsi_ < 30 => "oversold"
    close > sma3 => "bullish"
    close < sma3 => "bearish"
    => "neutral"
