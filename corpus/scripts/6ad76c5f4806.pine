//@version=5
indicator("Na Default")
myFunc(float x = na) =>
    na(x) ? -1.0 : x

var float r1 = 0.0
var float r2 = 0.0
r1 := myFunc()       // x=na → -1.0
r2 := myFunc(42.0)   // x=42.0
plot(close)
