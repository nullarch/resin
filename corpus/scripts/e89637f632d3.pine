
//@version=5
indicator("MA Consensus")
bullCount = 0
for period = 3 to 5
    ma = ta.sma(close, period)
    if close > ma
        bullCount := bullCount + 1
consensus = bullCount >= 2
plot(bullCount, "BullCount")
plot(consensus ? 1 : 0, "Consensus")
