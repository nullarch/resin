//@version=5
indicator("Var Reassign")
float x = 10.0
if close > 100
    x := 20.0
// x should be 20.0 after if (PineScript: no block scope for reassignment)
var float result = 0.0
result := x
plot(result)
