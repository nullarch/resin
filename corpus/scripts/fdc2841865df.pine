//@version=5
indicator("Array build")
arr = array.new_float(0)
for i = 0 to 9
    array.push(arr, close + float(i))
sz = array.size(arr)
mn = array.min(arr)
mx = array.max(arr)
rng = array.range(arr)
med = array.median(arr)
