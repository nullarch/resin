
//@version=5
indicator("Valid Accum")
sum = 0.0
validCount = 0
for i = 0 to 7
    val = close[i]
    if not na(val) and val > 100
        sum := sum + val
        validCount := validCount + 1
avg = validCount > 0 ? sum / validCount : 0.0
plot(avg, "Avg")
