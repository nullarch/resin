
//@version=5
indicator("Mom Comp")
rsi = nz(ta.rsi(close, 3), 50.0)
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
rsiScore = (rsi - 50) / 50
trendScore = sma5 > 0 ? (sma3 - sma5) / sma5 * 100 : 0.0
composite = rsiScore * 0.6 + trendScore * 0.4
plot(composite, "C")
