//@version=5
indicator("ForIn Map TA")
var m = map.new<string, float>()
map.put(m, "fast", 3.0)
map.put(m, "slow", 5.0)
var float lastSma = 0.0
for [key, period] in m
    sma_ = ta.sma(close, int(period))
    if not na(sma_)
        lastSma := sma_
plot(lastSma)
