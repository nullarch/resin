//@version=5
library("MathUtils")
export add(float a, float b) =>
    a + b
export subtract(float a, float b) =>
    a - b
export clamp(float val, float lo, float hi) =>
    math.max(lo, math.min(hi, val))
