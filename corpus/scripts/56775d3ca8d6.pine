
//@version=5
indicator("Park Vol")
hl = high > 0 and low > 0 ? math.log(high / low) : 0.0
hlSq = math.pow(hl, 2)
parkVar = ta.sma(hlSq, 5) / (4 * math.log(2))
parkVol = math.sqrt(math.abs(parkVar)) * math.sqrt(252)
plot(parkVol, "PV")
