//@version=5
strategy("Position track", overlay=true)
sma3 = ta.sma(close, 3)
sma5 = ta.sma(close, 5)
if ta.crossover(sma3, sma5)
    strategy.entry("Long", strategy.long)
if ta.crossunder(sma3, sma5)
    strategy.entry("Short", strategy.short)
posSize = strategy.position_size
